{%- liquid
  assign helix_header_id = 'helix-header-' | append: section.id
  assign helix_logo_width = section.settings.logo_width | default: 110
  assign helix_mobile_logo_width = section.settings.mobile_logo_width | default: 90
  assign helix_logo_text = section.settings.logo_text | default: 'HELIX'
  assign helix_currency_code = localization.country.currency.iso_code | default: 'VND'
  assign helix_currency_symbol = localization.country.currency.symbol | default: '₫'
  assign helix_announcement_1 = section.settings.announcement_message_1 | default: 'Free shipping for all orders over 5.000.000₫'
  assign helix_announcement_1 = helix_announcement_1 | replace: 'Free shipping for all orders over 5.000.000₫Free shipping for all orders over 5.000.000₫', 'Free shipping for all orders over 5.000.000₫'
  assign helix_announcement_1 = helix_announcement_1 | replace: 'Free shipping for all orders over 5.000.000₫Free shipping for all orders over', 'Free shipping for all orders over 5.000.000₫'
  if helix_announcement_1 contains 'Free shipping for all orders over'
    assign helix_announcement_1 = 'Free shipping for all orders over 5.000.000₫'
  endif
  assign helix_announcement_2 = section.settings.announcement_message_2 | default: 'Receive 20% off your first order. Shop now'
  assign helix_emphasis_1 = section.settings.announcement_emphasis_1 | default: '5.000.000₫'
  assign helix_emphasis_2 = section.settings.announcement_emphasis_2
  assign helix_collections_url = routes.collections_url
  assign helix_women_url = section.settings.women_url
  if helix_women_url == blank
    assign helix_women_url = helix_collections_url | append: '/all-womens'
  endif
  assign helix_men_url = section.settings.men_url
  if helix_men_url == blank
    assign helix_men_url = helix_collections_url | append: '/all-mens'
  endif
  assign helix_find_shoes_url = section.settings.find_shoes_url | default: '/pages/find-your-shoes'
  assign helix_journal_url = section.settings.pages_journal_url | default: '/blogs/news'
  assign helix_womens_top_picks_url = helix_collections_url | append: '/womens-top-picks'
  assign helix_womens_best_sellers_url = section.settings.best_sellers_url
  if helix_womens_best_sellers_url == blank
    assign helix_womens_best_sellers_url = helix_collections_url | append: '/womens-best-sellers'
  endif
  assign helix_womens_trending_url = section.settings.trending_url
  if helix_womens_trending_url == blank
    assign helix_womens_trending_url = helix_collections_url | append: '/womens-trending'
  endif
  assign helix_womens_new_arrivals_url = helix_collections_url | append: '/womens-new-arrivals'
  assign helix_womens_sale_url = section.settings.sale_url
  if helix_womens_sale_url == blank
    assign helix_womens_sale_url = helix_collections_url | append: '/womens-on-sale'
  endif
  assign helix_mens_top_picks_url = helix_collections_url | append: '/mens-top-picks'
  assign helix_mens_best_sellers_url = section.settings.best_sellers_url
  if helix_mens_best_sellers_url == blank
    assign helix_mens_best_sellers_url = helix_collections_url | append: '/mens-best-sellers'
  endif
  assign helix_mens_trending_url = section.settings.trending_url
  if helix_mens_trending_url == blank
    assign helix_mens_trending_url = helix_collections_url | append: '/mens-trending'
  endif
  assign helix_mens_new_arrivals_url = helix_collections_url | append: '/mens-new-arrivals'
  assign helix_mens_sale_url = section.settings.sale_url
  if helix_mens_sale_url == blank
    assign helix_mens_sale_url = helix_collections_url | append: '/mens-on-sale'
  endif
  assign helix_men_featured_handles = blank
  if section.settings.men_featured_collection != blank
    assign helix_men_featured_handles = section.settings.men_featured_collection.products | map: 'handle'
  endif
  if helix_men_featured_handles == blank
    assign helix_men_featured_handles = 'prime-form-men|base-line-men|form-core-men|frame-one-men|drift-prime-men' | split: '|'
  endif
  assign helix_trending_collection_url = section.settings.trending_url
  if helix_trending_collection_url == blank
    assign helix_trending_collection_url = helix_collections_url | append: '/trending'
  endif
  assign helix_best_sellers_collection_url = section.settings.best_sellers_url
  if helix_best_sellers_collection_url == blank
    assign helix_best_sellers_collection_url = helix_collections_url | append: '/best-sellers'
  endif
  assign helix_sale_collection_url = section.settings.sale_url
  if helix_sale_collection_url == blank
    assign helix_sale_collection_url = helix_collections_url | append: '/sale'
  endif
  assign helix_mobile_logo = section.settings.mobile_logo
  if helix_mobile_logo == blank
    assign helix_mobile_logo = section.settings.logo
  endif
  assign helix_mobile_transparent_logo = section.settings.mobile_transparent_logo
  if helix_mobile_transparent_logo == blank
    assign helix_mobile_transparent_logo = section.settings.transparent_logo
  endif
  if helix_mobile_transparent_logo == blank
    assign helix_mobile_transparent_logo = helix_mobile_logo
  endif
-%}

<div
  id="{{ helix_header_id }}"
  class="helix-header{% if section.settings.overlay_header %} helix-header--overlay{% endif %}{% if section.settings.sticky_header %} helix-header--sticky{% endif %}{% if section.settings.show_header_border %} helix-header--border{% endif %}{% unless section.settings.show_announcement %} helix-header--no-announcement{% endunless %}"
  data-helix-header
  data-announcement-interval="{{ section.settings.announcement_interval | default: 5 | times: 1000 }}"
  style="
    --helix-overlay-text: {{ section.settings.overlay_text_color | default: '#ffffff' }};
    --helix-solid-text: {{ section.settings.solid_text_color | default: '#323232' }};
    --helix-header-bg: {{ section.settings.header_background | default: '#ffffff' }};
    --helix-header-border: {{ section.settings.header_border_color | default: '#e5e5e5' }};
    --helix-announcement-bg: {{ section.settings.announcement_background | default: '#d3f285' }};
    --helix-announcement-text: {{ section.settings.announcement_text_color | default: '#1a1312' }};
    --helix-logo-width: {{ helix_logo_width }}px;
    --helix-mobile-logo-width: {{ helix_mobile_logo_width }}px;
    --helix-desktop-side-padding: {{ section.settings.desktop_side_padding | default: 50 }}px;
    --helix-mega-max-height: {{ section.settings.mega_menu_max_height | default: 540 }}px;
  "
>
  {% if section.settings.show_announcement %}
    <div class="helix-header__announcement" data-helix-announcement role="region" aria-label="Announcements">
      <div class="helix-header__announcement-side helix-header__announcement-side--left">
        <button type="button" data-helix-announcement-prev aria-label="{{ 'header.previous_announcement' | t }}">
          <span aria-hidden="true">{% render 'helix-icon-chevron' %}</span>
        </button>
      </div>

      <div class="helix-header__announcement-track" aria-live="polite">
        <p class="helix-header__announcement-message is-active" data-helix-announcement-message>
          {% if helix_emphasis_1 != blank and helix_announcement_1 contains helix_emphasis_1 %}
            {% assign helix_announcement_1_prefix = helix_announcement_1 | split: helix_emphasis_1 | first %}
            {% assign helix_announcement_1_suffix = helix_announcement_1 | split: helix_emphasis_1 | last %}
            {{ helix_announcement_1_prefix }}<strong>{{ helix_emphasis_1 }}</strong>{{ helix_announcement_1_suffix }}
          {% else %}
            {{ helix_announcement_1 }}
          {% endif %}
        </p>
        {% if helix_announcement_2 != blank %}
          <p class="helix-header__announcement-message" data-helix-announcement-message>
            {% if helix_emphasis_2 != blank and helix_announcement_2 contains helix_emphasis_2 %}
              {% assign helix_announcement_2_prefix = helix_announcement_2 | split: helix_emphasis_2 | first %}
              {% assign helix_announcement_2_suffix = helix_announcement_2 | split: helix_emphasis_2 | last %}
              {{ helix_announcement_2_prefix }}<strong>{{ helix_emphasis_2 }}</strong>{{ helix_announcement_2_suffix }}
            {% else %}
              {{ helix_announcement_2 }}
            {% endif %}
          </p>
        {% endif %}
      </div>

      <div class="helix-header__announcement-side helix-header__announcement-side--right">
        <button type="button" data-helix-announcement-next aria-label="{{ 'header.next_announcement' | t }}">
          <span aria-hidden="true">{% render 'helix-icon-chevron' %}</span>
        </button>
      </div>
    </div>
  {% endif %}

  <div class="helix-header__desktop-bar" data-helix-desktop-bar>
    <div class="helix-header__menu-background" data-helix-menu-background aria-hidden="true"></div>
    <div class="helix-header__desktop-surface">
      <div class="helix-header__desktop-inner">
        <nav class="helix-header__desktop-nav" aria-label="{{ 'header.primary_navigation' | t }}">
          <details class="helix-header__dropdown helix-header__dropdown--mega" data-helix-dropdown data-helix-hover-dropdown>
            <summary class="helix-header__nav-trigger" data-url="{{ helix_women_url }}">
              <span>{{ section.settings.women_label | default: 'Women' }}</span>
              <span class="helix-header__chevron" aria-hidden="true">{% render 'helix-icon-chevron' %}</span>
            </summary>
            <div class="helix-header__mega-panel" data-helix-mega-panel>
              <div class="helix-header__mega-inner">
                <div class="helix-header__mega-columns">
                  <div class="helix-header__mega-links-grid">
                    <div class="helix-header__mega-column">
                      <ul class="helix-header__mega-links" role="list">
                        <li><a href="{{ helix_womens_top_picks_url }}">Top picks</a></li>
                        <li><a href="{{ helix_womens_best_sellers_url }}">Best sellers</a></li>
                        <li><a href="{{ helix_womens_trending_url }}">Trending</a></li>
                        <li><a href="{{ helix_womens_new_arrivals_url }}">New arrivals</a></li>
                        <li><a href="{{ helix_womens_sale_url }}">On sale</a></li>
                      </ul>
                    </div>
                    <div class="helix-header__mega-column">
                      <p class="helix-header__mega-title">By activity</p>
                      <ul class="helix-header__mega-links" role="list">
                        <li><a href="{{ helix_collections_url }}/womens-hiking">Hiking</a></li>
                        <li><a href="{{ helix_collections_url }}/women-s-running">Running</a></li>
                        <li><a href="{{ helix_collections_url }}/women-s-training">Training</a></li>
                        <li><a href="{{ helix_collections_url }}/womens-tennis">Tennis</a></li>
                        <li><a href="{{ helix_collections_url }}/womens-lifestyle">Lifestyle</a></li>
                        <li><a href="{{ helix_women_url }}">Shop all</a></li>
                      </ul>
                    </div>
                    <div class="helix-header__mega-column">
                      <p class="helix-header__mega-title">By feature</p>
                      <ul class="helix-header__mega-links" role="list">
                        <li><a href="{{ helix_women_url }}?filter.p.m.custom.feature=Cushioned">Cushioned</a></li>
                        <li><a href="{{ helix_women_url }}?filter.p.m.custom.feature=Waterproof">Waterproof</a></li>
                        <li><a href="{{ helix_women_url }}?filter.p.m.custom.feature=Water-resistant">Water-resistant</a></li>
                        <li><a href="{{ helix_women_url }}?filter.p.m.custom.feature=Breathable">Breathable</a></li>
                        <li><a href="{{ helix_women_url }}?filter.p.m.custom.feature=Windproof">Windproof</a></li>
                      </ul>
                    </div>
                  </div>
                  <div class="helix-header__mega-banners">
                    <a class="helix-header__mega-promo" href="{{ helix_collections_url }}/womens-on-sale">
                      {% if section.settings.women_banner != blank %}
                        {{ section.settings.women_banner | image_url: width: 900 | image_tag: loading: 'lazy', widths: '360,540,720,900', sizes: '(min-width: 991px) 24vw, 100vw', alt: section.settings.women_banner_title }}
                      {% else %}
                        {% # theme-check-disable RemoteAsset %}
                        <img src="https://helix-shoes-theme.myshopify.com/cdn/shop/files/banner_mega_menu_1.webp?v=1777884363&amp;width=900" width="900" height="604" loading="lazy" alt="">
                        {% # theme-check-enable RemoteAsset %}
                      {% endif %}
                      <span class="helix-header__mega-promo-content">
                        <strong>{% if section.settings.women_banner != blank %}{{ section.settings.women_banner_title | default: 'Find your favourites' }}{% else %}Women's sale{% endif %}</strong>
                        <span>{{ 'header.shop_now' | t }} ↗</span>
                      </span>
                    </a>
                    <a class="helix-header__mega-promo" href="{{ helix_women_url }}?sort_by=best-selling">
                      {% # theme-check-disable RemoteAsset %}
                      <img src="https://helix-shoes-theme.myshopify.com/cdn/shop/files/banner_mega_menu_2.webp?v=1777884363&amp;width=900" width="900" height="604" loading="lazy" alt="">
                      {% # theme-check-enable RemoteAsset %}
                      <span class="helix-header__mega-promo-content">
                        <strong>Women’s favorites</strong>
                        <span>{{ 'header.shop_now' | t }} ↗</span>
                      </span>
                    </a>
                  </div>
                </div>
              </div>
            </div>
          </details>

          <details class="helix-header__dropdown helix-header__dropdown--mega" data-helix-dropdown data-helix-hover-dropdown>
            <summary class="helix-header__nav-trigger" data-url="{{ helix_men_url }}">
              <span>{{ section.settings.men_label | default: 'Men' }}</span>
              <span class="helix-header__chevron" aria-hidden="true">{% render 'helix-icon-chevron' %}</span>
            </summary>
            <div class="helix-header__mega-panel" data-helix-mega-panel>
              <div class="helix-header__mega-inner">
                <div class="helix-header__mega-columns">
                  <div class="helix-header__mega-links-grid">
                    <div class="helix-header__mega-column">
                      <ul class="helix-header__mega-links" role="list">
                      <li><a href="{{ helix_mens_top_picks_url }}">Top picks</a></li>
                      <li><a href="{{ helix_mens_best_sellers_url }}">Best sellers</a></li>
                      <li><a href="{{ helix_mens_trending_url }}">Trending</a></li>
                      <li><a href="{{ helix_mens_new_arrivals_url }}">New arrivals</a></li>
                      <li><a href="{{ helix_mens_sale_url }}">On sale</a></li>
                      </ul>
                    </div>
                    <div class="helix-header__mega-column">
                      <p class="helix-header__mega-title">By activity</p>
                      <ul class="helix-header__mega-links" role="list">
                      <li><a href="{{ helix_collections_url }}/mens-hiking">Hiking</a></li>
                      <li><a href="{{ helix_collections_url }}/mens-running">Running</a></li>
                      <li><a href="{{ helix_collections_url }}/mens-training">Training</a></li>
                      <li><a href="{{ helix_collections_url }}/mens-tennis">Tennis</a></li>
                      <li><a href="{{ helix_collections_url }}/mens-lifestyle">Lifestyle</a></li>
                      <li><a href="{{ helix_men_url }}">Shop all</a></li>
                      </ul>
                    </div>
                    <div class="helix-header__mega-column">
                      <p class="helix-header__mega-title">By feature</p>
                      <ul class="helix-header__mega-links" role="list">
                      <li><a href="{{ helix_men_url }}?filter.p.m.custom.feature=Cushioned">Cushioned</a></li>
                      <li><a href="{{ helix_men_url }}?filter.p.m.custom.feature=Waterproof">Waterproof</a></li>
                      <li><a href="{{ helix_men_url }}?filter.p.m.custom.feature=Water-resistant">Water-resistant</a></li>
                      <li><a href="{{ helix_men_url }}?filter.p.m.custom.feature=Breathable">Breathable</a></li>
                      <li><a href="{{ helix_men_url }}?filter.p.m.custom.feature=Windproof">Windproof</a></li>
                      </ul>
                    </div>
                  </div>
                  <div class="helix-header__mega-products" data-helix-men-products>
                    <div class="helix-header__mega-products-head">
                      <p class="helix-header__mega-title">Best deals <span aria-hidden="true">🔥</span></p>
                      <div class="helix-header__mega-products-controls" aria-label="Men products">
                        <button type="button" class="helix-header__mega-products-button helix-header__mega-products-button--prev" data-helix-men-products-prev aria-label="Previous" disabled>
                          {% render 'helix-icon-chevron' %}
                        </button>
                        <button type="button" class="helix-header__mega-products-button helix-header__mega-products-button--next" data-helix-men-products-next aria-label="Next">
                          {% render 'helix-icon-chevron' %}
                        </button>
                      </div>
                    </div>

                    <div class="helix-header__mega-products-viewport" data-helix-men-products-viewport>
                      <div class="helix-header__mega-products-track" data-helix-men-products-track>
        {% for helix_men_featured_handle in helix_men_featured_handles %}
          {% assign helix_men_product = all_products[helix_men_featured_handle] %}
          {% if helix_men_product == blank and collections.all.products[forloop.index0] != blank %}
            {% assign helix_men_product = collections.all.products[forloop.index0] %}
          {% endif %}
          {% if helix_men_product != blank %}
            {% assign helix_men_product_variant = helix_men_product.selected_or_first_available_variant %}
            {% assign helix_men_compare_at_price = helix_men_product_variant.compare_at_price %}
            {% assign helix_men_discount = 0 %}
            {% if helix_men_compare_at_price > helix_men_product_variant.price %}
              {% assign helix_men_discount_difference = helix_men_compare_at_price | minus: helix_men_product_variant.price %}
              {% assign helix_men_discount = helix_men_discount_difference | times: 100 | divided_by: helix_men_compare_at_price | round %}
            {% endif %}
            {% assign helix_men_secondary_image = helix_men_product.images[1] %}
            {% assign helix_men_gallery_variant_image = helix_men_product.images[2] %}
            <article class="helix-header__mega-product">
              <div class="helix-header__mega-product-media">
                <a href="{{ helix_men_product.url }}" aria-label="{{ helix_men_product.title | escape }}">
                  {% if helix_men_product.featured_image != blank %}
                    {{ helix_men_product.featured_image | image_url: width: 700 | image_tag: loading: 'lazy', widths: '294,441,588,700', sizes: '294px', alt: helix_men_product.title, class: 'helix-header__mega-product-image' }}
                  {% endif %}
                  {% if helix_men_secondary_image != blank %}
                    {{ helix_men_secondary_image | image_url: width: 700 | image_tag: loading: 'lazy', widths: '294,441,588,700', sizes: '294px', alt: helix_men_product.title, class: 'helix-header__mega-product-image helix-header__mega-product-image--secondary' }}
                  {% endif %}
                </a>
                {% if helix_men_discount > 0 %}
                  <span class="helix-header__mega-product-badge">-{{ helix_men_discount }}%</span>
                {% endif %}
                <a class="helix-header__mega-product-quick-view" href="{{ helix_men_product.url }}" aria-label="Quick view">
                  <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                    <path d="M1.718 10.291a.75.75 0 0 1 0-.582A9.04 9.04 0 0 1 10 4.168a9.04 9.04 0 0 1 8.282 5.541.75.75 0 0 1 0 .582A9.04 9.04 0 0 1 10 15.832a9.04 9.04 0 0 1-8.282-5.541Z" stroke="currentColor" stroke-width="1.25" stroke-linecap="round" stroke-linejoin="round"/>
                    <circle cx="10" cy="10" r="2.5" stroke="currentColor" stroke-width="1.25"/>
                  </svg>
                </a>
                <a class="helix-header__mega-product-quick-add" href="{{ helix_men_product.url }}">Quick add</a>
              </div>
              <div class="helix-header__mega-product-details">
                {% if helix_men_secondary_image != blank or helix_men_gallery_variant_image != blank %}
                  <ul class="helix-header__mega-product-swatches" role="list" aria-label="Product gallery">
                    {% if helix_men_secondary_image != blank %}
                      <li>
                        <a href="{{ helix_men_product.url }}" aria-label="View {{ helix_men_product.title | escape }} image 2">
                          <span title="Gallery image 2" style="--helix-swatch-image: url('{{ helix_men_secondary_image | image_url: width: 80 }}');"></span>
                        </a>
                      </li>
                    {% endif %}
                    {% if helix_men_gallery_variant_image != blank %}
                      <li>
                        <a href="{{ helix_men_product.url }}" aria-label="View {{ helix_men_product.title | escape }} image 3">
                          <span title="Gallery image 3" style="--helix-swatch-image: url('{{ helix_men_gallery_variant_image | image_url: width: 80 }}');"></span>
                        </a>
                      </li>
                    {% endif %}
                  </ul>
                {% endif %}
                <div class="helix-header__mega-product-info">
                  <a href="{{ helix_men_product.url }}">{{ helix_men_product.title }}</a>
                  <div>
                    <span{% if helix_men_compare_at_price > helix_men_product_variant.price %} class="helix-header__mega-product-sale-price"{% endif %}>{{ helix_men_product_variant.price | money }}</span>
                    {% if helix_men_compare_at_price > helix_men_product_variant.price %}
                      <s>{{ helix_men_compare_at_price | money }}</s>
                    {% endif %}
                  </div>
                </div>
              </div>
            </article>
          {% endif %}
        {% endfor %}
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </details>

          <details class="helix-header__dropdown helix-header__dropdown--pages" data-helix-dropdown data-helix-hover-dropdown>
            <summary class="helix-header__nav-trigger">
              <span>{{ section.settings.pages_label | default: 'Pages' }}</span>
              <span class="helix-header__chevron" aria-hidden="true">{% render 'helix-icon-chevron' %}</span>
            </summary>
            <div class="helix-header__pages-panel">
              <a href="{{ section.settings.pages_about_url | default: '/pages/about-us' }}">About us</a>
              <a href="{{ section.settings.pages_contact_url | default: '/pages/contact' }}">Contact</a>
              <a href="{{ section.settings.pages_faq_url | default: '/pages/faqs' }}">Faqs</a>
              <a href="{{ section.settings.pages_store_url | default: '/pages/store-locations' }}">Store locations</a>
              <a href="{{ section.settings.pages_recent_url | default: '/pages/recently-viewed' }}">Recently viewed</a>
              <a href="{{ helix_journal_url }}">Our journal</a>
            </div>
          </details>

          <a class="helix-header__nav-link" href="{{ helix_find_shoes_url }}">{{ section.settings.find_shoes_label | default: 'Find your shoes' }}</a>
        </nav>

        <a class="helix-header__brand" href="{{ routes.root_url }}" aria-label="{{ helix_logo_text }}">
          {% if section.settings.logo != blank %}
            <span class="helix-header__logo helix-header__logo--normal">
              {{ section.settings.logo | image_url: width: 300 | image_tag: loading: 'eager', widths: '110,220,300', sizes: '110px', alt: helix_logo_text }}
            </span>
            <span class="helix-header__logo helix-header__logo--overlay" aria-hidden="true">
              {% if section.settings.transparent_logo != blank %}
                {{ section.settings.transparent_logo | image_url: width: 300 | image_tag: loading: 'eager', widths: '110,220,300', sizes: '110px', alt: '' }}
              {% else %}
                {{ section.settings.logo | image_url: width: 300 | image_tag: loading: 'eager', widths: '110,220,300', sizes: '110px', alt: '' }}
              {% endif %}
            </span>
          {% else %}
            <span class="helix-header__logo helix-header__logo--normal">
              {% # theme-check-disable RemoteAsset %}
              <img src="https://helix-shoes-theme.myshopify.com/cdn/shop/files/logo.svg?v=1776243138&amp;width=220" width="110" height="19" loading="eager" alt="{{ helix_logo_text }}">
              {% # theme-check-enable RemoteAsset %}
            </span>
            <span class="helix-header__logo helix-header__logo--overlay" aria-hidden="true">
              {% # theme-check-disable RemoteAsset %}
              <img src="https://helix-shoes-theme.myshopify.com/cdn/shop/files/logo-white.svg?v=1776243137&amp;width=220" width="110" height="19" loading="eager" alt="">
              {% # theme-check-enable RemoteAsset %}
            </span>
          {% endif %}
        </a>

        <div class="helix-header__actions">
          {% if section.settings.show_currency != false %}
            <details class="helix-header__dropdown helix-header__dropdown--currency" data-helix-dropdown data-helix-hover-dropdown>
              <summary class="helix-header__currency-trigger">
                <span class="helix-header__country-flag" aria-hidden="true" style="--helix-country-flag: url(//cdn.shopify.com/static/images/flags/{{ localization.country.iso_code | default: 'VN' | downcase }}.svg?width=20);"></span>
                <span>{{ helix_currency_code }} {{ helix_currency_symbol }}</span>
                <span class="helix-header__chevron" aria-hidden="true">{% render 'helix-icon-chevron' %}</span>
              </summary>
              <div class="helix-header__currency-panel">
                <p class="visually-hidden">{{ 'header.country_region' | t }}</p>
                <form action="{{ routes.localization_url }}" method="post" data-helix-localization-form data-helix-localization-type="country">
                  <input type="hidden" name="return_to" value="{{ request.path }}">
                  <input type="hidden" name="country_code" value="{{ localization.country.iso_code | default: 'VN' }}" data-helix-country-value>
                  <div class="helix-header__country-filter">
                    <label class="visually-hidden" for="{{ helix_header_id }}-country-filter">{{ 'header.search_country' | t }}</label>
                    <input id="{{ helix_header_id }}-country-filter" class="helix-header__country-filter-input" type="search" placeholder="{{ 'header.search_country' | t }}" autocomplete="off" data-helix-country-filter>
                    <span class="helix-header__country-filter-icon" aria-hidden="true">{% render 'helix-icon-search' %}</span>
                  </div>
                  <div class="helix-header__country-list" role="list">
                    {% for country in localization.available_countries %}
                      <button type="button" value="{{ country.iso_code }}" data-helix-country-option role="listitem">
                        <span class="helix-header__country-option-name">
                          <span class="helix-header__country-option-flag" aria-hidden="true" style="--helix-country-flag: url(//cdn.shopify.com/static/images/flags/{{ country.iso_code | downcase }}.svg?width=20);"></span>
                          <span>{{ country.name }}</span>
                        </span>
                        <span class="helix-header__country-option-currency">{{ country.currency.iso_code }} {{ country.currency.symbol }}</span>
                      </button>
                    {% endfor %}
                  </div>
                </form>
              </div>
            </details>
          {% endif %}
          {% if section.settings.show_currency != false and section.settings.show_search != false or section.settings.show_account != false or section.settings.show_cart != false %}
            <span class="helix-header__actions-divider" aria-hidden="true"></span>
          {% endif %}

          {% if section.settings.show_search != false %}
            <a class="helix-header__icon-button" href="{{ routes.search_url }}" data-helix-open-search aria-label="{{ 'header.search' | t }}">
              {% render 'helix-icon-search' %}
              <span class="visually-hidden">{{ 'header.search' | t }}</span>
            </a>
          {% endif %}
          {% if section.settings.show_account != false %}
            <a class="helix-header__icon-button helix-header__account-link" href="{{ routes.account_login_url }}" aria-label="{{ 'header.account' | t }}">
              {% render 'helix-icon-account' %}
              <span class="visually-hidden">{{ 'header.account' | t }}</span>
            </a>
          {% endif %}
          {% if section.settings.show_cart != false %}
            <a class="helix-header__icon-button helix-header__cart-link" href="{{ routes.cart_url }}" data-helix-open-cart aria-label="{{ 'header.cart' | t }}">
              <span class="helix-header__cart-icon">
                {% render 'helix-icon-cart' %}
                {% if cart.item_count > 0 %}<span class="helix-header__cart-count">{{ cart.item_count }}</span>{% endif %}
              </span>
              <span class="visually-hidden">{{ 'header.cart' | t }}</span>
            </a>
          {% endif %}
        </div>
      </div>
    </div>
  </div>

  <button class="helix-header__mega-scrim" type="button" data-helix-mega-scrim data-helix-close-mega aria-hidden="true" aria-label="{{ 'general.close' | t }}"></button>

  <div class="helix-header__mobile-bar" data-helix-mobile-bar>
    <div class="helix-header__mobile-inner">
      <div class="helix-header__mobile-side helix-header__mobile-side--left">
        <button class="helix-header__mobile-icon" type="button" data-helix-mobile-open aria-expanded="false" aria-controls="{{ helix_header_id }}-mobile-menu" aria-label="{{ 'header.open_menu' | t }}">
          {% render 'helix-icon-menu' %}
        </button>
      </div>

      <a class="helix-header__brand helix-header__brand--mobile" href="{{ routes.root_url }}" aria-label="{{ helix_logo_text }}">
        {% if helix_mobile_logo != blank %}
          <span class="helix-header__logo helix-header__logo--normal">
            {{ helix_mobile_logo | image_url: width: 240 | image_tag: loading: 'eager', widths: '90,180,240', sizes: '90px', alt: helix_logo_text }}
          </span>
          <span class="helix-header__logo helix-header__logo--overlay" aria-hidden="true">
            {% if helix_mobile_transparent_logo != blank %}
              {{ helix_mobile_transparent_logo | image_url: width: 240 | image_tag: loading: 'eager', widths: '90,180,240', sizes: '90px', alt: '' }}
            {% else %}
              {{ helix_mobile_logo | image_url: width: 240 | image_tag: loading: 'eager', widths: '90,180,240', sizes: '90px', alt: '' }}
            {% endif %}
          </span>
        {% else %}
          <span class="helix-header__logo helix-header__logo--normal">
            {% # theme-check-disable RemoteAsset %}
            <img src="https://helix-shoes-theme.myshopify.com/cdn/shop/files/logo.svg?v=1776243138&amp;width=180" width="90" height="16" loading="eager" alt="{{ helix_logo_text }}">
            {% # theme-check-enable RemoteAsset %}
          </span>
          <span class="helix-header__logo helix-header__logo--overlay" aria-hidden="true">
            {% # theme-check-disable RemoteAsset %}
            <img src="https://helix-shoes-theme.myshopify.com/cdn/shop/files/logo-white.svg?v=1776243137&amp;width=180" width="90" height="16" loading="eager" alt="">
            {% # theme-check-enable RemoteAsset %}
          </span>
        {% endif %}
      </a>

      <div class="helix-header__mobile-side helix-header__mobile-side--right">
        {% if section.settings.show_search != false %}
          <a class="helix-header__mobile-icon" href="{{ routes.search_url }}" data-helix-open-search aria-label="{{ 'header.search' | t }}">
            {% render 'helix-icon-search' %}
          </a>
        {% endif %}
        {% if section.settings.show_cart != false %}
          <a class="helix-header__mobile-icon helix-header__cart-link" href="{{ routes.cart_url }}" data-helix-open-cart aria-label="{{ 'header.cart' | t }}">
            <span class="helix-header__cart-icon">
              {% render 'helix-icon-cart' %}
              {% if cart.item_count > 0 %}<span class="helix-header__cart-count">{{ cart.item_count }}</span>{% endif %}
            </span>
          </a>
        {% endif %}
      </div>
    </div>
  </div>

  <button class="helix-header__scrim" type="button" data-helix-close-layer aria-label="{{ 'general.close' | t }}"></button>

  <div class="helix-header__mobile-menu" id="{{ helix_header_id }}-mobile-menu" data-helix-mobile-menu aria-hidden="true">
    <div class="helix-header__mobile-menu-panel" role="dialog" aria-modal="true" aria-label="{{ 'header.menu' | t }}" tabindex="-1">
      <div class="helix-header__mobile-menu-top">
        <a class="helix-header__mobile-menu-logo" href="{{ routes.root_url }}" aria-label="{{ helix_logo_text }}">
          {% if helix_mobile_logo != blank %}
            {{ helix_mobile_logo | image_url: width: 240 | image_tag: loading: 'eager', widths: '90,180,240', sizes: '90px', alt: helix_logo_text }}
          {% else %}
            {% # theme-check-disable RemoteAsset %}
            <img src="https://helix-shoes-theme.myshopify.com/cdn/shop/files/logo.svg?v=1776243138&amp;width=180" width="90" height="16" loading="eager" alt="{{ helix_logo_text }}">
            {% # theme-check-enable RemoteAsset %}
          {% endif %}
        </a>
        <button class="helix-header__close-button" type="button" data-helix-close-mobile aria-label="{{ 'general.close' | t }}">
          {% render 'helix-icon-close' %}
        </button>
      </div>
      <nav class="helix-header__mobile-nav" aria-label="{{ 'header.primary_navigation' | t }}">
        <details data-helix-mobile-section>
          <summary class="helix-header__mobile-nav-summary helix-header__mobile-nav-summary--split">
            <a class="helix-header__mobile-title-link" href="{{ helix_women_url }}">{{ section.settings.women_label | default: 'Women' }}</a>
            <span class="helix-header__mobile-toggle" aria-hidden="true">{% render 'helix-icon-mobile-chevron' %}</span>
          </summary>
          <div class="helix-header__mobile-submenu helix-header__mobile-submenu--mega">
            <div class="helix-header__mobile-submenu-header">
              <button class="helix-header__mobile-back" type="button" data-helix-mobile-back aria-label="{{ 'general.back' | t }}">
                <span aria-hidden="true">{% render 'helix-icon-mobile-chevron' %}</span>
                <span>{{ section.settings.women_label | default: 'Women' }}</span>
              </button>
              <button class="helix-header__close-button" type="button" data-helix-close-mobile aria-label="{{ 'general.close' | t }}">
                {% render 'helix-icon-close' %}
              </button>
            </div>
            <div class="helix-header__mobile-submenu-inner">
              <div class="helix-header__mobile-submenu-group helix-header__mobile-submenu-group--featured">
                <a href="{{ helix_womens_top_picks_url }}">Top picks</a>
                <a href="{{ helix_womens_best_sellers_url }}">Best sellers</a>
                <a href="{{ helix_womens_trending_url }}">Trending</a>
                <a href="{{ helix_womens_new_arrivals_url }}">New arrivals</a>
                <a href="{{ helix_womens_sale_url }}">On sale</a>
              </div>
              <div class="helix-header__mobile-submenu-list">
              <details class="helix-header__mobile-submenu-details">
                <summary><span>By activity</span><span aria-hidden="true">{% render 'helix-icon-mobile-chevron' %}</span></summary>
                <div class="helix-header__mobile-submenu helix-header__mobile-submenu--child">
                  <div class="helix-header__mobile-submenu-header">
                    <button class="helix-header__mobile-back" type="button" data-helix-mobile-back aria-label="{{ 'general.back' | t }}">
                      <span aria-hidden="true">{% render 'helix-icon-mobile-chevron' %}</span>
                      <span>By activity</span>
                    </button>
                    <button class="helix-header__close-button" type="button" data-helix-close-mobile aria-label="{{ 'general.close' | t }}">
                      {% render 'helix-icon-close' %}
                    </button>
                  </div>
                  <div class="helix-header__mobile-submenu-inner">
                    <div class="helix-header__mobile-submenu-group">
                      <a href="{{ helix_collections_url }}/womens-hiking">Hiking</a>
                      <a href="{{ helix_collections_url }}/women-s-running">Running</a>
                      <a href="{{ helix_collections_url }}/women-s-training">Training</a>
                      <a href="{{ helix_collections_url }}/womens-tennis">Tennis</a>
                      <a href="{{ helix_collections_url }}/womens-lifestyle">Lifestyle</a>
                      <a href="{{ helix_women_url }}">Shop all</a>
                    </div>
                  </div>
                </div>
              </details>
              <details class="helix-header__mobile-submenu-details">
                <summary><span>By feature</span><span aria-hidden="true">{% render 'helix-icon-mobile-chevron' %}</span></summary>
                <div class="helix-header__mobile-submenu helix-header__mobile-submenu--child">
                  <div class="helix-header__mobile-submenu-header">
                    <button class="helix-header__mobile-back" type="button" data-helix-mobile-back aria-label="{{ 'general.back' | t }}">
                      <span aria-hidden="true">{% render 'helix-icon-mobile-chevron' %}</span>
                      <span>By feature</span>
                    </button>
                    <button class="helix-header__close-button" type="button" data-helix-close-mobile aria-label="{{ 'general.close' | t }}">
                      {% render 'helix-icon-close' %}
                    </button>
                  </div>
                  <div class="helix-header__mobile-submenu-inner">
                    <div class="helix-header__mobile-submenu-group">
                      <a href="{{ helix_women_url }}?filter.p.m.custom.feature=Cushioned">Cushioned</a>
                      <a href="{{ helix_women_url }}?filter.p.m.custom.feature=Waterproof">Waterproof</a>
                      <a href="{{ helix_women_url }}?filter.p.m.custom.feature=Water-resistant">Water-resistant</a>
                      <a href="{{ helix_women_url }}?filter.p.m.custom.feature=Breathable">Breathable</a>
                      <a href="{{ helix_women_url }}?filter.p.m.custom.feature=Windproof">Windproof</a>
                    </div>
                  </div>
                </div>
              </details>
              </div>
              <div class="helix-header__mobile-promos">
                <a class="helix-header__mobile-promo" href="{{ helix_collections_url }}/womens-on-sale">
                  <span class="helix-header__mobile-promo-media">
                    {% # theme-check-disable RemoteAsset %}
                    <img src="https://helix-shoes-theme.myshopify.com/cdn/shop/files/banner_mega_menu_1.webp?v=1777884363&amp;width=450" width="450" height="302" loading="lazy" alt="">
                    {% # theme-check-enable RemoteAsset %}
                  </span>
                  <div class="helix-header__mobile-promo-details">
                    <div class="helix-header__mobile-promo-details-inner">
                      <strong>Women's sale</strong>
                      <span>
                        <span class="helix-header__mobile-promo-cta-text">Shop now</span>
                        <span class="helix-header__mobile-promo-cta-icon" aria-hidden="true">
                          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M7 7H17M17 7V17M17 7L7 17" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"></path></svg>
                        </span>
                      </span>
                    </div>
                  </div>
                </a>
                <a class="helix-header__mobile-promo" href="{{ helix_women_url }}?sort_by=best-selling">
                  <span class="helix-header__mobile-promo-media">
                    {% # theme-check-disable RemoteAsset %}
                    <img src="https://helix-shoes-theme.myshopify.com/cdn/shop/files/banner_mega_menu_2.webp?v=1777884363&amp;width=450" width="450" height="302" loading="lazy" alt="">
                    {% # theme-check-enable RemoteAsset %}
                  </span>
                  <div class="helix-header__mobile-promo-details">
                    <div class="helix-header__mobile-promo-details-inner">
                      <strong>Women’s favorites</strong>
                      <span>
                        <span class="helix-header__mobile-promo-cta-text">Shop now</span>
                        <span class="helix-header__mobile-promo-cta-icon" aria-hidden="true">
                          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M7 7H17M17 7V17M17 7L7 17" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"></path></svg>
                        </span>
                      </span>
                    </div>
                  </div>
                </a>
              </div>
            </div>
          </div>
        </details>
        <details data-helix-mobile-section>
          <summary class="helix-header__mobile-nav-summary helix-header__mobile-nav-summary--split">
            <a class="helix-header__mobile-title-link" href="{{ helix_men_url }}">{{ section.settings.men_label | default: 'Men' }}</a>
            <span class="helix-header__mobile-toggle" aria-hidden="true">{% render 'helix-icon-mobile-chevron' %}</span>
          </summary>
          <div class="helix-header__mobile-submenu helix-header__mobile-submenu--mega">
            <div class="helix-header__mobile-submenu-header">
              <button class="helix-header__mobile-back" type="button" data-helix-mobile-back aria-label="{{ 'general.back' | t }}">
                <span aria-hidden="true">{% render 'helix-icon-mobile-chevron' %}</span>
                <span>{{ section.settings.men_label | default: 'Men' }}</span>
              </button>
              <button class="helix-header__close-button" type="button" data-helix-close-mobile aria-label="{{ 'general.close' | t }}">
                {% render 'helix-icon-close' %}
              </button>
            </div>
            <div class="helix-header__mobile-submenu-inner">
              <div class="helix-header__mobile-submenu-group helix-header__mobile-submenu-group--featured">
                <a href="{{ helix_mens_top_picks_url }}">Top picks</a>
                <a href="{{ helix_mens_best_sellers_url }}">Best sellers</a>
                <a href="{{ helix_mens_trending_url }}">Trending</a>
                <a href="{{ helix_mens_new_arrivals_url }}">New arrivals</a>
                <a href="{{ helix_mens_sale_url }}">On sale</a>
              </div>
              <div class="helix-header__mobile-submenu-list">
              <details class="helix-header__mobile-submenu-details">
                <summary><span>By activity</span><span aria-hidden="true">{% render 'helix-icon-mobile-chevron' %}</span></summary>
                <div class="helix-header__mobile-submenu helix-header__mobile-submenu--child">
                  <div class="helix-header__mobile-submenu-header">
                    <button class="helix-header__mobile-back" type="button" data-helix-mobile-back aria-label="{{ 'general.back' | t }}">
                      <span aria-hidden="true">{% render 'helix-icon-mobile-chevron' %}</span>
                      <span>By activity</span>
                    </button>
                    <button class="helix-header__close-button" type="button" data-helix-close-mobile aria-label="{{ 'general.close' | t }}">
                      {% render 'helix-icon-close' %}
                    </button>
                  </div>
                  <div class="helix-header__mobile-submenu-inner">
                    <div class="helix-header__mobile-submenu-group">
                      <a href="{{ helix_collections_url }}/mens-hiking">Hiking</a>
                      <a href="{{ helix_collections_url }}/mens-running">Running</a>
                      <a href="{{ helix_collections_url }}/mens-training">Training</a>
                      <a href="{{ helix_collections_url }}/mens-tennis">Tennis</a>
                      <a href="{{ helix_collections_url }}/mens-lifestyle">Lifestyle</a>
                      <a href="{{ helix_men_url }}">Shop all</a>
                    </div>
                  </div>
                </div>
              </details>
              <details class="helix-header__mobile-submenu-details">
                <summary><span>By feature</span><span aria-hidden="true">{% render 'helix-icon-mobile-chevron' %}</span></summary>
                <div class="helix-header__mobile-submenu helix-header__mobile-submenu--child">
                  <div class="helix-header__mobile-submenu-header">
                    <button class="helix-header__mobile-back" type="button" data-helix-mobile-back aria-label="{{ 'general.back' | t }}">
                      <span aria-hidden="true">{% render 'helix-icon-mobile-chevron' %}</span>
                      <span>By feature</span>
                    </button>
                    <button class="helix-header__close-button" type="button" data-helix-close-mobile aria-label="{{ 'general.close' | t }}">
                      {% render 'helix-icon-close' %}
                    </button>
                  </div>
                  <div class="helix-header__mobile-submenu-inner">
                    <div class="helix-header__mobile-submenu-group">
                      <a href="{{ helix_men_url }}?filter.p.m.custom.feature=Cushioned">Cushioned</a>
                      <a href="{{ helix_men_url }}?filter.p.m.custom.feature=Waterproof">Waterproof</a>
                      <a href="{{ helix_men_url }}?filter.p.m.custom.feature=Water-resistant">Water-resistant</a>
                      <a href="{{ helix_men_url }}?filter.p.m.custom.feature=Breathable">Breathable</a>
                      <a href="{{ helix_men_url }}?filter.p.m.custom.feature=Windproof">Windproof</a>
                    </div>
                  </div>
                </div>
              </details>
              </div>
              <div class="helix-header__mobile-products">
                <p class="helix-header__mobile-products-title">Best deals <span aria-hidden="true">🔥</span></p>
                <div class="helix-header__mobile-products-grid">
                  {% for helix_men_mobile_handle in helix_men_featured_handles limit: 5 %}
                    {% assign helix_men_mobile_product = all_products[helix_men_mobile_handle] %}
                    {% if helix_men_mobile_product == blank and collections.all.products[forloop.index0] != blank %}
                      {% assign helix_men_mobile_product = collections.all.products[forloop.index0] %}
                    {% endif %}
                    {% if helix_men_mobile_product != blank %}
                      {% assign helix_men_mobile_variant = helix_men_mobile_product.selected_or_first_available_variant %}
                      {% assign helix_men_mobile_compare_at = helix_men_mobile_variant.compare_at_price %}
                      {% assign helix_men_mobile_discount = 0 %}
                      {% if helix_men_mobile_compare_at > helix_men_mobile_variant.price %}
                        {% assign helix_men_mobile_difference = helix_men_mobile_compare_at | minus: helix_men_mobile_variant.price %}
                        {% assign helix_men_mobile_discount = helix_men_mobile_difference | times: 100 | divided_by: helix_men_mobile_compare_at | round %}
                      {% endif %}
                      {% assign helix_men_mobile_secondary = helix_men_mobile_product.images[1] %}
                      {% assign helix_men_mobile_gallery_variant_image = helix_men_mobile_product.images[2] %}
                      <article class="helix-header__mega-product helix-header__mobile-product">
                        <div class="helix-header__mega-product-media">
                          <a href="{{ helix_men_mobile_product.url }}" aria-label="{{ helix_men_mobile_product.title | escape }}">
                            {% if helix_men_mobile_product.featured_image != blank %}
                              {{ helix_men_mobile_product.featured_image | image_url: width: 700 | image_tag: loading: 'lazy', widths: '294,441,588,700', sizes: '160px', alt: helix_men_mobile_product.title, class: 'helix-header__mega-product-image' }}
                            {% endif %}
                            {% if helix_men_mobile_secondary != blank %}
                              {{ helix_men_mobile_secondary | image_url: width: 700 | image_tag: loading: 'lazy', widths: '294,441,588,700', sizes: '160px', alt: helix_men_mobile_product.title, class: 'helix-header__mega-product-image helix-header__mega-product-image--secondary' }}
                            {% endif %}
                          </a>
                          {% if helix_men_mobile_discount > 0 %}<span class="helix-header__mega-product-badge">-{{ helix_men_mobile_discount }}%</span>{% endif %}
                          <a class="helix-header__mega-product-quick-view" href="{{ helix_men_mobile_product.url }}" aria-label="Quick view">
                            {% render 'helix-icon-cart' %}
                          </a>
                        </div>
                        <div class="helix-header__mega-product-details">
                          {% if helix_men_mobile_secondary != blank or helix_men_mobile_gallery_variant_image != blank %}
                            <ul class="helix-header__mega-product-swatches" role="list" aria-label="Product gallery">
                              {% if helix_men_mobile_secondary != blank %}
                                <li><a href="{{ helix_men_mobile_product.url }}" aria-label="View {{ helix_men_mobile_product.title | escape }} image 2"><span title="Gallery image 2" style="--helix-swatch-image: url('{{ helix_men_mobile_secondary | image_url: width: 80 }}');"></span></a></li>
                              {% endif %}
                              {% if helix_men_mobile_gallery_variant_image != blank %}
                                <li><a href="{{ helix_men_mobile_product.url }}" aria-label="View {{ helix_men_mobile_product.title | escape }} image 3"><span title="Gallery image 3" style="--helix-swatch-image: url('{{ helix_men_mobile_gallery_variant_image | image_url: width: 80 }}');"></span></a></li>
                              {% endif %}
                            </ul>
                          {% endif %}
                          <div class="helix-header__mega-product-info">
                            <a href="{{ helix_men_mobile_product.url }}">{{ helix_men_mobile_product.title }}</a>
                            <div>
                              <span{% if helix_men_mobile_compare_at > helix_men_mobile_variant.price %} class="helix-header__mega-product-sale-price"{% endif %}>{{ helix_men_mobile_variant.price | money }}</span>
                              {% if helix_men_mobile_compare_at > helix_men_mobile_variant.price %}<s>{{ helix_men_mobile_compare_at | money }}</s>{% endif %}
                            </div>
                          </div>
                        </div>
                      </article>
                    {% endif %}
                  {% endfor %}
                </div>
              </div>
            </div>
          </div>
        </details>
        <details data-helix-mobile-section>
          <summary class="helix-header__mobile-nav-summary">
            <span class="helix-header__mobile-summary-label">{{ section.settings.pages_label | default: 'Pages' }}</span>
            <span class="helix-header__mobile-toggle" aria-hidden="true">{% render 'helix-icon-mobile-chevron' %}</span>
          </summary>
          <div class="helix-header__mobile-submenu">
            <div class="helix-header__mobile-submenu-header">
              <button class="helix-header__mobile-back" type="button" data-helix-mobile-back aria-label="{{ 'general.back' | t }}">
                <span aria-hidden="true">{% render 'helix-icon-mobile-chevron' %}</span>
                <span>{{ section.settings.pages_label | default: 'Pages' }}</span>
              </button>
              <button class="helix-header__close-button" type="button" data-helix-close-mobile aria-label="{{ 'general.close' | t }}">
                {% render 'helix-icon-close' %}
              </button>
            </div>
            <div class="helix-header__mobile-submenu-inner">
              <div class="helix-header__mobile-submenu-group helix-header__mobile-submenu-group--featured helix-header__mobile-submenu-group--pages">
                <a href="{{ section.settings.pages_about_url | default: '/pages/about-us' }}">About us</a>
                <a href="{{ section.settings.pages_contact_url | default: '/pages/contact' }}">Contact</a>
                <a href="{{ section.settings.pages_faq_url | default: '/pages/faqs' }}">Faqs</a>
                <a href="{{ section.settings.pages_store_url | default: '/pages/store-locations' }}">Store locations</a>
                <a href="{{ section.settings.pages_recent_url | default: '/pages/recently-viewed' }}">Recently viewed</a>
                <a href="{{ helix_journal_url }}">Our journal</a>
              </div>
            </div>
          </div>
        </details>
        <a href="{{ helix_find_shoes_url }}">{{ section.settings.find_shoes_label | default: 'Find your shoes' }}</a>
      </nav>
      <div class="helix-header__mobile-menu-footer">
        {% if section.settings.show_account != false %}
          <div class="helix-header__mobile-account-row">
            <a class="helix-header__mobile-login" href="{{ routes.account_login_url }}">{{ 'header.login' | t | default: 'Login' }}</a>
          </div>
        {% endif %}
        <div class="helix-header__mobile-localization-row">
          {% if section.settings.show_currency != false %}
            <button class="helix-header__mobile-localization-trigger" type="button" data-helix-open-localization="country" aria-expanded="false" aria-controls="{{ helix_header_id }}-country-sheet">
              <span class="helix-header__country-flag" aria-hidden="true" style="--helix-country-flag: url(//cdn.shopify.com/static/images/flags/{{ localization.country.iso_code | default: 'VN' | downcase }}.svg?width=20);"></span>
              <span>{{ helix_currency_code }} {{ helix_currency_symbol }}</span>
              <span aria-hidden="true">{% render 'helix-icon-chevron' %}</span>
            </button>
          {% endif %}
          <button class="helix-header__mobile-localization-trigger" type="button" data-helix-open-localization="language" aria-expanded="false" aria-controls="{{ helix_header_id }}-language-sheet">
            <span>{{ localization.language.endonym_name | default: localization.language.name | default: 'English' }}</span>
            <span aria-hidden="true">{% render 'helix-icon-chevron' %}</span>
          </button>
        </div>
        <div class="helix-header__mobile-socials" aria-label="Social links">
          <a href="{{ routes.root_url }}" aria-label="Facebook">
            <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false"><path d="M14 8h3V4h-3c-3.3 0-5 1.9-5 5v3H6v4h3v4h4v-4h3l1-4h-4V9c0-.7.3-1 1-1Z" fill="currentColor"></path></svg>
          </a>
          <a href="{{ routes.root_url }}" aria-label="Instagram">
            <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false"><rect x="4" y="4" width="16" height="16" rx="4" fill="none" stroke="currentColor" stroke-width="2"></rect><circle cx="12" cy="12" r="3.5" fill="none" stroke="currentColor" stroke-width="2"></circle><circle cx="17.2" cy="6.8" r="1.2" fill="currentColor"></circle></svg>
          </a>
          <a href="{{ routes.root_url }}" aria-label="YouTube">
            <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false"><path d="M21 8.2a2.8 2.8 0 0 0-2-2C17.2 5.7 12 5.7 12 5.7s-5.2 0-7 .5a2.8 2.8 0 0 0-2 2A29 29 0 0 0 2.5 12 29 29 0 0 0 3 15.8a2.8 2.8 0 0 0 2 2c1.8.5 7 .5 7 .5s5.2 0 7-.5a2.8 2.8 0 0 0 2-2 29 29 0 0 0 .5-3.8 29 29 0 0 0-.5-3.8ZM10 15.2V8.8l5.5 3.2-5.5 3.2Z" fill="currentColor"></path></svg>
          </a>
          <a href="{{ routes.root_url }}" aria-label="TikTok">
            <svg viewBox="0 0 24 24" aria-hidden="true" focusable="false"><path d="M14.5 4c.4 2.3 1.7 3.7 4 4v3.1a8.4 8.4 0 0 1-4-1.2v6.4a5.2 5.2 0 1 1-4.6-5.2v3.2a2.1 2.1 0 1 0 1.5 2V4h3.1Z" fill="currentColor"></path></svg>
          </a>
        </div>
      </div>
    </div>
  </div>

  <div class="helix-header__localization-sheet" id="{{ helix_header_id }}-country-sheet" data-helix-localization-sheet="country" aria-hidden="true">
    <button class="helix-header__localization-sheet-overlay" type="button" data-helix-close-localization aria-label="{{ 'general.close' | t }}"></button>
    <div class="helix-header__localization-sheet-container" role="dialog" aria-modal="true" aria-label="{{ 'header.country' | t }}" tabindex="-1">
      <span class="helix-header__localization-sheet-handle" aria-hidden="true"></span>
      <div class="helix-header__localization-sheet-header helix-header__localization-sheet-header--country">
        <h2>{{ 'header.country' | t }}</h2>
        <button class="helix-header__localization-close-button" type="button" data-helix-close-localization aria-label="{{ 'general.close' | t }}">{% render 'helix-icon-close' %}</button>
      </div>
      <div class="helix-header__localization-sheet-inner">
        <form action="{{ routes.localization_url }}" method="post" data-helix-localization-form data-helix-localization-type="country">
          <input type="hidden" name="return_to" value="{{ request.path }}">
          <input type="hidden" name="country_code" value="{{ localization.country.iso_code | default: 'VN' }}" data-helix-country-value>
          <div class="helix-header__country-filter">
            <label class="visually-hidden" for="{{ helix_header_id }}-mobile-country-filter">{{ 'header.search_country' | t }}</label>
            <input id="{{ helix_header_id }}-mobile-country-filter" class="helix-header__country-filter-input" type="search" placeholder="{{ 'header.search_country' | t }}" autocomplete="off" data-helix-country-filter>
            <span class="helix-header__country-filter-icon" aria-hidden="true">{% render 'helix-icon-search' %}</span>
          </div>
          <div class="helix-header__country-list" role="list">
            {% for country in localization.available_countries %}
              <button type="button" value="{{ country.iso_code }}" data-helix-country-option role="listitem">
                <span class="helix-header__country-option-name">
                  <span class="helix-header__country-option-flag" aria-hidden="true" style="--helix-country-flag: url(//cdn.shopify.com/static/images/flags/{{ country.iso_code | downcase }}.svg?width=20);"></span>
                  <span>{{ country.name }}</span>
                </span>
                <span class="helix-header__country-option-currency">{{ country.currency.iso_code }} {{ country.currency.symbol }}</span>
              </button>
            {% endfor %}
          </div>
        </form>
      </div>
    </div>
  </div>

  <div class="helix-header__localization-sheet helix-header__localization-sheet--language" id="{{ helix_header_id }}-language-sheet" data-helix-localization-sheet="language" aria-hidden="true">
    <button class="helix-header__localization-sheet-overlay" type="button" data-helix-close-localization aria-label="{{ 'general.close' | t }}"></button>
    <div class="helix-header__localization-sheet-container" role="dialog" aria-modal="true" aria-label="{{ 'header.language' | t }}" tabindex="-1">
      <span class="helix-header__localization-sheet-handle" aria-hidden="true"></span>
      <div class="helix-header__localization-sheet-header">
        <h2>{{ 'header.language' | t }}</h2>
        <button class="helix-header__localization-close-button" type="button" data-helix-close-localization aria-label="{{ 'general.close' | t }}">{% render 'helix-icon-close' %}</button>
      </div>
      <div class="helix-header__localization-sheet-inner helix-header__localization-sheet-inner--language">
        <form action="{{ routes.localization_url }}" method="post" data-helix-localization-form data-helix-localization-type="language">
          <input type="hidden" name="return_to" value="{{ request.path }}">
          <input type="hidden" name="language_code" value="{{ localization.language.iso_code | default: 'en' }}" data-helix-language-value>
          <div class="helix-header__language-list" role="list">
            {% for language in localization.available_languages %}
              <button type="button" value="{{ language.iso_code }}" data-helix-language-option role="listitem">
                <span>{{ language.endonym_name | default: language.name }}</span>
              </button>
            {% endfor %}
          </div>
        </form>
      </div>
    </div>
  </div>

  <div class="helix-header__drawer helix-header__drawer--search" data-helix-search-drawer aria-hidden="true">
    <div class="helix-header__drawer-panel" role="dialog" aria-modal="true" aria-label="{{ 'header.search' | t }}" tabindex="-1">
      <div class="helix-header__drawer-top">
        <strong>{{ 'header.search' | t }}</strong>
        <button class="helix-header__close-button" type="button" data-helix-close-layer aria-label="{{ 'general.close' | t }}">{% render 'helix-icon-close' %}</button>
      </div>
      <div class="helix-header__drawer-inner helix-header__drawer-inner--search">
        <form action="{{ routes.search_url }}" method="get" class="helix-header__search-form">
          <label class="visually-hidden" for="{{ helix_header_id }}-search">{{ 'header.search' | t }}</label>
          <input id="{{ helix_header_id }}-search" type="search" name="q" placeholder="{{ 'header.search' | t }}" autocomplete="off">
          <button type="submit" aria-label="{{ 'header.search' | t }}">{% render 'helix-icon-search' %}</button>
        </form>
        <div class="helix-header__search-popular">
          <h3>{{ 'header.popular_searches' | t }}</h3>
          <div class="helix-header__search-links">
            <a href="{{ routes.search_url }}?type=product&amp;options%5Bprefix%5D=last&amp;q=Running+shoes">Running shoes</a>
            <a href="{{ routes.search_url }}?type=product&amp;options%5Bprefix%5D=last&amp;q=Trail+running+shoes">Trail running shoes</a>
            <a href="{{ routes.search_url }}?type=product&amp;options%5Bprefix%5D=last&amp;q=Hiking+shoes">Hiking shoes</a>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="helix-header__drawer helix-header__drawer--cart" data-helix-cart-drawer aria-hidden="true">
    <div class="helix-header__drawer-panel" role="dialog" aria-modal="true" aria-label="{{ 'header.cart' | t }}" tabindex="-1">
      <div class="helix-header__drawer-top">
        <strong>{{ 'header.cart' | t }}</strong>
        <button class="helix-header__close-button" type="button" data-helix-close-layer aria-label="{{ 'general.close' | t }}">{% render 'helix-icon-close' %}</button>
      </div>
      <div class="helix-header__drawer-inner helix-header__drawer-inner--cart">
        {% if cart.item_count > 0 %}
          <div class="helix-header__cart-summary">
            <p class="helix-header__cart-message">{{ cart.item_count }} {{ 'header.items_in_cart' | t }}</p>
            <div class="helix-header__cart-total">{{ cart.total_price | money }}</div>
            <a class="helix-header__drawer-button helix-header__drawer-button--dark" href="{{ routes.cart_url }}">{{ 'header.view_cart' | t }}</a>
            {% if additional_checkout_buttons %}{{ content_for_additional_checkout_buttons }}{% endif %}
          </div>
        {% else %}
          <div class="helix-header__cart-empty">
            <h3>{{ 'header.empty_cart' | t }}</h3>
            <p>{{ 'header.explore_collections' | t }}</p>
          </div>
          <div class="helix-header__cart-collections">
            <a class="helix-header__drawer-button" href="{{ helix_trending_collection_url }}">{{ 'header.trending' | t }}</a>
            <a class="helix-header__drawer-button" href="{{ helix_best_sellers_collection_url }}">{{ 'header.best_sellers' | t }}</a>
            <a class="helix-header__drawer-button" href="{{ helix_sale_collection_url }}">{{ 'header.on_sale' | t }}</a>
          </div>
        {% endif %}
      </div>
    </div>
  </div>
</div>

{% style %}
  @import url('https://fonts.googleapis.com/css2?family=Archivo:wght@700&family=Figtree:wght@400;500;600;700&display=swap');

  #{{ helix_header_id }} {
    --helix-font: {% if section.settings.font_family != blank %}{{ section.settings.font_family.family }}, {{ section.settings.font_family.fallback_families }}{% else %}'Figtree', sans-serif{% endif %};
  }
{% endstyle %}

{% stylesheet %}
  .helix-header,
  .helix-header * {
    box-sizing: border-box;
  }

  .helix-header {
    position: relative;
    z-index: 30;
    width: 100%;
    color: var(--helix-solid-text);
    font-family: var(--helix-font, 'Figtree', 'Open Sans', Arial, sans-serif);
    font-size: 14px;
    line-height: 1.6;
  }

  body:has(.helix-header) {
    overflow-x: hidden;
  }

  html.helix-header-lock,
  html.helix-header-lock body {
    overflow: hidden;
  }

  html.helix-header-lock body {
    padding-right: var(--helix-scrollbar-compensation, 0px);
  }

  .helix-header button,
  .helix-header input {
    color: inherit;
    font: inherit;
  }

  .helix-header button,
  .helix-header summary {
    cursor: pointer;
  }

  .helix-header a {
    color: inherit;
    text-decoration: none;
  }

  .helix-header__announcement {
    display: grid;
    min-height: 38.4px;
    grid-template-columns: 32px minmax(0, 700px) 32px;
    align-items: center;
    justify-content: center;
    padding: 0 16px;
    overflow: hidden;
    background: var(--helix-announcement-bg);
    color: var(--helix-announcement-text);
    font-size: 14px;
    line-height: 22.4px;
  }

  .helix-header__announcement-track {
    position: relative;
    grid-column: 2;
    width: 100%;
    min-width: 0;
    height: 22.4px;
    min-height: 22.4px;
    overflow: hidden;
    text-align: center;
  }

  .helix-header__announcement-message {
    position: absolute;
    inset: 0;
    margin: 0;
    width: 100%;
    white-space: nowrap;
    opacity: 0;
    transform: translateY(7px);
    transition: opacity .2s cubic-bezier(.6, .14, 0, 1), transform .3s cubic-bezier(.6, .14, 0, 1);
    pointer-events: none;
  }

  .helix-header__announcement-message.is-active {
    opacity: 1;
    transform: translateY(0);
  }

  .helix-header__announcement-message strong {
    font-weight: 700;
  }

  .helix-header__announcement-side {
    display: flex;
    width: 32px;
    height: 38.4px;
    align-items: center;
    justify-content: center;
  }

  .helix-header__announcement-side--right {
    grid-column: 3;
  }

  .helix-header__announcement-side button {
    display: grid;
    width: 32px;
    height: 38.4px;
    padding: 0;
    place-items: center;
    border: 0;
    background: transparent;
    font-size: 14px;
    line-height: 22.4px;
    transition: transform .2s ease, opacity .2s ease;
  }

  .helix-header__announcement-side--left {
    grid-column: 1;
  }

  .helix-header__announcement-side--left button span {
    display: inline-flex;
    transform: rotate(90deg);
  }

  .helix-header__announcement-side--right button span {
    display: inline-flex;
    transform: rotate(-90deg);
  }

  .helix-header__announcement-side button svg {
    width: 16px;
    height: 16px;
  }

  .helix-header__announcement-side button:hover,
  .helix-header__announcement-side button:focus-visible {
    transform: translateX(3px);
    opacity: .62;
  }

  .helix-header__announcement-side--left button:hover,
  .helix-header__announcement-side--left button:focus-visible {
    transform: translateX(-3px);
  }

  .helix-header__desktop-bar,
  .helix-header__mobile-bar {
    position: absolute;
    z-index: 3;
    top: 38.4px;
    right: 0;
    left: 0;
    color: var(--helix-solid-text);
  }

  .helix-header--no-announcement .helix-header__desktop-bar,
  .helix-header--no-announcement .helix-header__mobile-bar {
    top: 0;
  }

  .helix-header__desktop-surface,
  .helix-header__mobile-inner {
    background: transparent;
    transition: background-color .2s cubic-bezier(.6, 0, .4, 1), box-shadow .2s cubic-bezier(.6, 0, .4, 1), border-color .2s cubic-bezier(.6, 0, .4, 1), color .2s cubic-bezier(.6, 0, .4, 1);
  }

  .helix-header--border .helix-header__desktop-surface,
  .helix-header--border .helix-header__mobile-inner {
    border-bottom: 1px solid var(--helix-header-border);
  }

  .helix-header:not(.helix-header--overlay) .helix-header__desktop-surface,
  .helix-header:not(.helix-header--overlay) .helix-header__mobile-inner {
    background: var(--helix-header-bg);
  }

  .helix-header__desktop-surface {
    position: relative;
    z-index: 1;
    isolation: isolate;
  }

  .helix-header__menu-background {
    position: absolute;
    z-index: 0;
    top: 0;
    right: 0;
    left: 0;
    height: 0;
    background: transparent;
    pointer-events: none;
    transition: height .3s cubic-bezier(.6, .14, 0, 1);
  }

  .helix-header__mega-scrim {
    position: fixed;
    z-index: 0;
    top: 0;
    right: 0;
    bottom: 0;
    left: 0;
    display: block;
    width: 100%;
    margin: 0;
    padding: 0;
    border: 0;
    background: rgba(50, 50, 50, .5);
    -webkit-backdrop-filter: blur(20px);
    backdrop-filter: blur(20px);
    opacity: 0;
    pointer-events: none;
    visibility: hidden;
    transition: opacity .3s ease, visibility 0s linear .3s;
  }

  .helix-header.is-mega-backdrop-open .helix-header__mega-scrim {
    opacity: 1;
    pointer-events: auto;
    visibility: visible;
    transition-delay: 0s;
  }

  .helix-header--no-announcement .helix-header__mega-scrim,
  .helix-header.is-scrolled .helix-header__mega-scrim {
    top: 0;
  }

  .helix-header__desktop-surface::before {
    display: none;
  }

  .helix-header__desktop-inner {
    position: relative;
    z-index: 1;
    display: grid;
    width: min(1820px, calc(100% - (var(--helix-desktop-side-padding) * 2)));
    min-height: 80px;
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
    align-items: center;
    margin-left: var(--helix-desktop-side-padding);
    margin-right: 0;
    gap: 10px;
    padding: 24px 0;
  }

  @media (min-width: 991px) and (max-width: 1199px) {
    .helix-header__desktop-inner {
      width: calc(100% - 60px);
      margin-left: 30px;
    }
  }

  .helix-header__desktop-nav,
  .helix-header__actions {
    display: flex;
    min-width: 0;
    align-items: center;
  }

  .helix-header__desktop-nav {
    justify-content: flex-start;
    gap: 0;
    margin-left: -12px;
  }

  .helix-header__actions {
    justify-content: flex-end;
    gap: 20px;
  }

  .helix-header__brand {
    position: relative;
    z-index: 2;
    display: inline-flex;
    width: var(--helix-logo-width);
    min-width: var(--helix-logo-width);
    height: auto;
    align-items: center;
    justify-content: center;
    justify-self: center;
    line-height: 1;
  }

  .helix-header__desktop-inner > .helix-header__brand {
    height: 19px;
  }

  .helix-header__brand--mobile {
    width: var(--helix-mobile-logo-width);
    min-width: var(--helix-mobile-logo-width);
  }

  .helix-header__logo,
  .helix-header__logo-text {
    display: block;
    width: 100%;
    line-height: 1;
  }

  .helix-header__logo img {
    display: block;
    width: 100%;
    height: auto;
  }

  .helix-header__logo--overlay,
  .helix-header__logo-text--overlay {
    position: absolute;
    inset: 0;
    display: none;
  }

  .helix-header__logo-text {
    font-size: 18px;
    font-weight: 700;
    letter-spacing: -.03em;
    text-align: center;
    white-space: nowrap;
  }

  .helix-header__nav-trigger,
  .helix-header__currency-trigger,
  .helix-header__nav-link {
    position: relative;
    display: inline-flex;
    min-height: 32px;
    align-items: center;
    gap: 4px;
    padding: 5px 12px;
    border: 0;
    background: transparent;
    font-size: 14px;
    font-weight: 500;
    letter-spacing: .02em;
    line-height: 22.4px;
    white-space: nowrap;
  }

  .helix-header__nav-trigger::after,
  .helix-header__nav-link::after,
  .helix-header__currency-trigger::after {
    position: absolute;
    right: 12px;
    bottom: 1px;
    left: 12px;
    height: 1px;
    background: currentColor;
    content: '';
    opacity: .85;
    transform: scaleX(0);
    transform-origin: right center;
    transition: transform .2s cubic-bezier(.6, 0, .4, 1);
  }

  .helix-header__nav-trigger:hover::after,
  .helix-header__nav-trigger:focus-visible::after,
  .helix-header__nav-link:hover::after,
  .helix-header__nav-link:focus-visible::after,
  .helix-header__dropdown[open] > .helix-header__nav-trigger::after,
  .helix-header__currency-trigger:hover::after,
  .helix-header__currency-trigger:focus-visible::after,
  .helix-header__dropdown[open] > .helix-header__currency-trigger::after {
    transform: scaleX(1);
    transform-origin: left center;
  }

  .helix-header__chevron {
    display: inline-flex;
    width: 20px;
    height: 20px;
    align-items: center;
    justify-content: center;
    transition: transform .3s cubic-bezier(.6, .14, 0, 1);
  }

  .helix-header__chevron svg {
    width: 20px;
    height: 20px;
  }

  .helix-header__desktop-nav .helix-header__chevron,
  .helix-header__desktop-nav .helix-header__chevron svg {
    width: 16px;
    height: 16px;
  }

  .helix-header__dropdown[open] > summary .helix-header__chevron {
    transform: rotate(180deg);
  }

  .helix-header__dropdown > summary::-webkit-details-marker {
    display: none;
  }

  .helix-header__dropdown > summary {
    list-style: none;
  }

  .helix-header__dropdown {
    position: static;
  }

  .helix-header__dropdown--pages,
  .helix-header__dropdown--currency {
    position: relative;
  }

  .helix-header__currency-trigger {
    display: flex;
    min-height: 16px;
    height: 16px;
    gap: 8px;
    padding: 0;
    font-weight: 400;
    line-height: 14px;
    letter-spacing: normal;
  }

  .helix-header__currency-trigger .helix-header__chevron,
  .helix-header__currency-trigger .helix-header__chevron svg {
    width: 16px;
    height: 16px;
  }

  .helix-header__country-flag {
    display: inline-block;
    width: 16px;
    height: 16px;
    flex: 0 0 16px;
    background: var(--helix-country-flag) center / contain no-repeat;
  }

  .helix-header__actions-divider {
    width: 1px;
    height: 16px;
    flex: 0 0 1px;
    margin-right: 0;
    border-right: 1px solid #e5e5e5;
  }

  .helix-header__dropdown--currency {
    margin-right: 0;
  }

  .helix-header__icon-button {
    display: inline-flex;
    width: 44px;
    height: 44px;
    align-items: center;
    justify-content: center;
    margin: -10px;
    padding: 10px;
    border: 0;
    background: transparent;
    transition: transform .25s ease, opacity .25s ease;
  }

  .helix-header__icon-button:hover,
  .helix-header__icon-button:focus-visible {
    opacity: .62;
    transform: translateY(-1px);
  }

  .helix-header__icon-button svg {
    width: 24px;
    height: 24px;
  }

  .helix-header__cart-icon {
    position: relative;
    display: inline-flex;
  }

  .helix-header__cart-count {
    position: absolute;
    top: -7px;
    right: -8px;
    display: grid;
    min-width: 15px;
    height: 15px;
    padding: 0 3px;
    place-items: center;
    border-radius: 50%;
    background: currentColor;
    color: var(--helix-announcement-bg);
    font-size: 9px;
    font-weight: 700;
    line-height: 1;
  }

  .helix-header__mega-panel {
    position: absolute;
    z-index: 8;
    top: 100%;
    right: -50px;
    left: -50px;
    height: var(--helix-mega-height, 0px);
    max-height: min(var(--helix-mega-max-height), calc(100vh - 80px));
    overflow: hidden;
    background: #fff;
    color: #1a1312;
    opacity: 0;
    pointer-events: none;
    transform: none;
    transition: height .3s cubic-bezier(.6, .14, 0, 1), opacity .2s cubic-bezier(.6, .14, 0, 1), visibility 0s linear .3s;
    visibility: hidden;
  }

  .helix-header__mega-panel::-webkit-scrollbar {
    width: 6px;
    height: 6px;
  }

  .helix-header__mega-panel::-webkit-scrollbar-thumb {
    border-radius: 99px;
    background: rgba(26, 19, 18, .2);
  }

  .helix-header__mega-panel::-webkit-scrollbar-track {
    background: transparent;
  }

  .helix-header__dropdown[open] > .helix-header__mega-panel {
    height: var(--helix-mega-height, min(540px, calc(100vh - 80px)));
    opacity: 1;
    overflow: auto;
    pointer-events: auto;
    transition-delay: 0s;
    visibility: visible;
  }

  .helix-header__mega-inner {
    width: 100%;
    margin-inline: auto;
    padding: 40px 0;
  }

  .helix-header__mega-columns {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 40px;
    padding-inline: clamp(30px, 3.94vw, 50px);
  }

  .helix-header__mega-links-grid {
    display: grid;
    width: 600px;
    max-width: 600px;
    flex: 1 1 0%;
    grid-template-columns: repeat(3, 200px);
    margin: -15px;
  }

  .helix-header__mega-banners {
    display: grid;
    min-width: 0;
    flex: 0 1 50%;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
  }

  .helix-header__mega-products {
    min-width: 0;
    flex: 0 1 50%;
    display: flex;
    flex-direction: column;
  }

  .helix-header__mega-products-head {
    display: flex;
    min-height: 25.2px;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    margin: 0 0 24px;
    opacity: 0;
    transform: translate3d(0, 15px, 0);
  }

  .helix-header__dropdown[open] .helix-header__mega-products-head {
    animation: helix-menu-reveal .4s ease .35s forwards;
  }

  .helix-header__mega-products-head .helix-header__mega-title {
    margin: 0;
    font-family: 'Archivo', var(--helix-font, sans-serif);
    font-size: 18px;
    font-weight: 700;
    letter-spacing: .36px;
    line-height: 25.2px;
  }

  .helix-header__mega-products-controls {
    display: flex;
    flex: 0 0 auto;
    align-items: center;
    gap: 10px;
  }

  .helix-header__mega-products-button {
    display: grid;
    width: 24px;
    height: 24px;
    padding: 0;
    place-items: center;
    border: 0;
    border-radius: 50%;
    background: transparent;
    color: currentColor;
    cursor: pointer;
    transition: opacity .2s ease, transform .2s ease;
  }

  .helix-header__mega-products-button:hover:not(:disabled),
  .helix-header__mega-products-button:focus-visible:not(:disabled) {
    opacity: .55;
    transform: translateY(-1px);
  }

  .helix-header__mega-products-button:disabled {
    cursor: not-allowed;
    opacity: .5;
  }

  .helix-header__mega-products-button svg {
    width: 18px;
    height: 18px;
  }

  .helix-header__mega-products-button--prev svg {
    transform: rotate(90deg);
  }

  .helix-header__mega-products-button--next svg {
    transform: rotate(-90deg);
  }

  .helix-header__mega-products-viewport {
    overflow: hidden;
    margin: 0 -10px;
    padding: 0 10px;
  }

  .helix-header__mega-products-track {
    display: flex;
    gap: 12px;
    transform: translate3d(0, 0, 0);
    transition: transform .35s cubic-bezier(.6, .14, 0, 1);
    will-change: transform;
  }

  .helix-header__mega-product {
    width: calc((100% - 24px) / 3);
    min-width: calc((100% - 24px) / 3);
    flex: 0 0 calc((100% - 24px) / 3);
    opacity: 0;
    transform: translate3d(0, 15px, 0);
  }

  .helix-header__dropdown[open] .helix-header__mega-product {
    animation: helix-menu-reveal .4s ease forwards;
  }

  .helix-header__dropdown[open] .helix-header__mega-product:nth-child(1) { animation-delay: .4s; }
  .helix-header__dropdown[open] .helix-header__mega-product:nth-child(2) { animation-delay: .45s; }
  .helix-header__dropdown[open] .helix-header__mega-product:nth-child(3) { animation-delay: .5s; }
  .helix-header__dropdown[open] .helix-header__mega-product:nth-child(4) { animation-delay: .55s; }
  .helix-header__dropdown[open] .helix-header__mega-product:nth-child(5) { animation-delay: .6s; }

  .helix-header__mega-product-media {
    position: relative;
    overflow: hidden;
    aspect-ratio: 1;
    border-radius: 16px;
    background: #f1f3f4;
  }

  .helix-header__mega-product-media > a:not(.helix-header__mega-product-quick-view):not(.helix-header__mega-product-quick-add) {
    position: relative;
    display: block;
    width: 100%;
    height: 100%;
  }

  .helix-header__mega-product-image {
    position: relative;
    display: block;
    width: 100%;
    height: 100%;
    object-fit: contain;
    transition: opacity .45s cubic-bezier(.6, .14, 0, 1);
  }

  .helix-header__mega-product-image--secondary {
    position: absolute;
    inset: 0;
    opacity: 0;
  }

  .helix-header__mega-product-media:hover .helix-header__mega-product-image--secondary,
  .helix-header__mega-product-media:focus-within .helix-header__mega-product-image--secondary {
    opacity: 1;
  }

  .helix-header__mega-product-badge {
    position: absolute;
    z-index: 2;
    top: 12px;
    left: 12px;
    display: inline-flex;
    min-height: 20px;
    align-items: center;
    padding: 3px 8.5px;
    border-radius: 9990px;
    background: #e80303;
    color: #fff;
    font-size: 12px;
    font-weight: 500;
    letter-spacing: .06em;
    line-height: normal;
  }

  .helix-header__mega-product-quick-view {
    position: absolute;
    z-index: 2;
    top: 12px;
    right: 12px;
    display: grid;
    width: 44px;
    height: 44px;
    align-items: center;
    justify-content: center;
    border-radius: 50%;
    background: #fff;
    color: #1a1312;
    opacity: 0;
    transform: translateX(10px);
    transition: opacity .3s cubic-bezier(.4, 0, .2, 1), transform .3s cubic-bezier(.4, 0, .2, 1);
  }

  .helix-header__mega-product-quick-view svg {
    width: 20px;
    height: 20px;
  }

  .helix-header__mega-product-media:hover .helix-header__mega-product-quick-view,
  .helix-header__mega-product-media:focus-within .helix-header__mega-product-quick-view {
    opacity: 1;
    transform: translateX(0);
  }

  .helix-header__mega-product-quick-add {
    position: absolute;
    z-index: 2;
    right: 12px;
    bottom: 12px;
    left: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 44px;
    padding: 6px 22px;
    border-radius: 9990px;
    background: #fff;
    color: #1a1312;
    font-size: 14px;
    font-weight: 500;
    letter-spacing: .28px;
    line-height: normal;
    opacity: 0;
    transform: translateY(10px);
    transition: opacity .25s ease, transform .35s cubic-bezier(.6, .14, 0, 1);
  }

  .helix-header__mega-product-media:hover .helix-header__mega-product-quick-add,
  .helix-header__mega-product-media:focus-within .helix-header__mega-product-quick-add {
    opacity: 1;
    transform: translateY(0);
  }

  .helix-header__mega-product-details {
    display: flex;
    width: 100%;
    min-width: 0;
    min-height: 113.78px;
    align-items: flex-start;
    flex-direction: column;
    gap: 8px;
    padding-top: 14px;
  }

  .helix-header__mega-product-swatches {
    display: flex;
    width: 88px;
    min-width: 88px;
    flex-wrap: wrap;
    align-items: flex-start;
    gap: 8px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .helix-header__mega-product-swatches li {
    display: flex;
    width: 40px;
    height: 45px;
    align-items: flex-start;
  }

  .helix-header__mega-product-swatches li a {
    display: flex;
    width: 40px;
    height: 40px;
    overflow: hidden;
    border-radius: 6px;
  }

  .helix-header__mega-product-swatches li span {
    display: block;
    width: 40px;
    height: 40px;
    border-radius: 6px;
    background: var(--helix-swatch-image) center / cover no-repeat;
  }

  .helix-header__mega-product-info {
    display: flex;
    width: 100%;
    min-width: 0;
    flex-direction: column;
    gap: 2px;
    color: #323232;
    font-size: 14px;
    line-height: 22.4px;
  }

  .helix-header__mega-product-info > a {
    display: -webkit-box;
    width: 100%;
    min-width: 0;
    min-height: 44.8px;
    max-width: 100%;
    overflow: hidden;
    color: #323232;
    font-size: 14px;
    font-weight: 500;
    line-height: 22.4px;
    text-overflow: ellipsis;
    overflow-wrap: anywhere;
    white-space: normal;
    line-clamp: 2;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
  }

  .helix-header__mega-product-info > div {
    display: flex;
    align-items: baseline;
    gap: 6px;
  }

  .helix-header__mega-product-sale-price {
    color: #d82727;
  }

  .helix-header__mega-product-info s {
    color: #131313;
    font-size: 85%;
    line-height: 1.6;
    opacity: .7;
  }

  .helix-header__mega-column,
  .helix-header__mega-promo {
    opacity: 0;
    transform: translate3d(0, 15px, 0);
  }

  .helix-header__mega-column {
    padding: 15px;
  }

  .helix-header__dropdown[open] .helix-header__mega-column,
  .helix-header__dropdown[open] .helix-header__mega-promo {
    animation: helix-menu-reveal .4s ease forwards;
  }

  .helix-header__dropdown[open] .helix-header__mega-column:nth-child(1) { animation-delay: .2s; }
  .helix-header__dropdown[open] .helix-header__mega-column:nth-child(2) { animation-delay: .25s; }
  .helix-header__dropdown[open] .helix-header__mega-column:nth-child(3) { animation-delay: .3s; }
  .helix-header__dropdown[open] .helix-header__mega-promo { animation-delay: .35s; }
  .helix-header__dropdown[open] .helix-header__mega-promo:nth-child(2) { animation-delay: .4s; }

  .helix-header__mega-title,
  .helix-header__panel-label {
    margin: 0 0 16px;
    color: #131313;
    font-size: 14px;
    font-weight: 500;
    letter-spacing: .02em;
    line-height: 22.4px;
  }

  .helix-header__mega-links {
    display: flex;
    flex-direction: column;
    gap: 10px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .helix-header__mega-links a,
  .helix-header__pages-panel a {
    position: relative;
    display: inline-flex;
    width: max-content;
    max-width: 100%;
    padding-block: 0;
    font-size: 14px;
    line-height: 22.4px;
    transition: color .2s ease;
  }

  .helix-header__mega-links a::after,
  .helix-header__pages-panel a::after {
    position: absolute;
    right: 0;
    bottom: -1px;
    left: 0;
    height: 1px;
    background: currentColor;
    content: '';
    transform: scaleX(0);
    transform-origin: right center;
    transition: transform .2s cubic-bezier(.6, 0, .4, 1);
  }

  .helix-header__mega-links a:hover::after,
  .helix-header__mega-links a:focus-visible::after,
  .helix-header__pages-panel a:hover::after,
  .helix-header__pages-panel a:focus-visible::after {
    transform: scaleX(1);
    transform-origin: left center;
  }

  .helix-header__mega-column:first-child .helix-header__mega-links a {
    font-family: 'Archivo', var(--helix-font, sans-serif);
    font-size: 16px;
    font-weight: 700;
    line-height: 25.6px;
  }

  .helix-header__mega-links a:hover,
  .helix-header__mega-links a:focus-visible,
  .helix-header__pages-panel a:hover,
  .helix-header__pages-panel a:focus-visible {
    color: rgba(26, 19, 18, .55);
  }

  .helix-header__mega-promo {
    display: flex;
    min-width: 0;
    flex-direction: column;
    gap: 16px;
    color: #1a1312;
  }

  .helix-header__mega-promo img {
    display: block;
    width: 100%;
    aspect-ratio: 1.49;
    height: auto;
    border-radius: 16px;
    object-fit: cover;
    transition: transform .55s cubic-bezier(.6, .14, 0, 1);
  }

  .helix-header__mega-promo:hover img {
    transform: scale(1.02);
  }

  .helix-header__mega-promo--fallback {
    display: none;
  }

  .helix-header__mega-promo-content {
    display: flex;
    width: 100%;
    flex-direction: column;
    gap: 8px;
  }

  .helix-header__mega-promo-content strong {
    font-family: 'Archivo', var(--helix-font, sans-serif);
    font-size: 16px;
    font-weight: 700;
    line-height: 24px;
  }

  .helix-header__mega-promo-content span {
    display: flex;
    min-height: 29px;
    align-items: center;
    font-size: 14px;
    font-weight: 500;
    letter-spacing: .28px;
    line-height: 22.4px;
  }

  .helix-header__pages-panel,
  .helix-header__currency-panel {
    position: absolute;
    z-index: 9;
    top: calc(100% + 6px);
    right: 0;
    width: max-content;
    min-width: 210px;
    padding: 15px;
    border: 1px solid rgba(26, 19, 18, .12);
    background: rgba(255, 255, 255, .98);
    box-shadow: 0 16px 40px rgba(26, 19, 18, .12);
    color: #1a1312;
    opacity: 0;
    pointer-events: none;
    transform: translateY(-7px);
    transition: opacity .2s cubic-bezier(.6, .14, 0, 1), transform .3s cubic-bezier(.6, .14, 0, 1), visibility 0s linear .3s;
    visibility: hidden;
  }

  .helix-header__dropdown[open] > .helix-header__pages-panel,
  .helix-header__dropdown[open] > .helix-header__currency-panel {
    opacity: 1;
    pointer-events: auto;
    transform: translateY(0);
    transition-delay: 0s;
    visibility: visible;
  }

  .helix-header__pages-panel {
    top: calc(100% + 24px);
    left: 0;
    right: auto;
    width: 270px;
    min-width: 270px;
    display: flex;
    flex-direction: column;
    gap: 2px;
    padding: 10px 0;
    border: 0;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, .1);
  }

  .helix-header__pages-panel a {
    width: 100%;
    max-width: none;
    min-height: 38.4px;
    align-items: center;
    padding: 8px 10px;
    opacity: 0;
    transform: translate3d(15px, 0, 0);
  }

  .helix-header__dropdown[open] > .helix-header__pages-panel a {
    animation: helix-menu-link-reveal .4s ease forwards;
  }

  .helix-header__dropdown[open] > .helix-header__pages-panel a:nth-child(1) { animation-delay: .15s; }
  .helix-header__dropdown[open] > .helix-header__pages-panel a:nth-child(2) { animation-delay: .2s; }
  .helix-header__dropdown[open] > .helix-header__pages-panel a:nth-child(3) { animation-delay: .25s; }
  .helix-header__dropdown[open] > .helix-header__pages-panel a:nth-child(4) { animation-delay: .3s; }
  .helix-header__dropdown[open] > .helix-header__pages-panel a:nth-child(5) { animation-delay: .35s; }
  .helix-header__dropdown[open] > .helix-header__pages-panel a:nth-child(6) { animation-delay: .4s; }

  .helix-header__country-list {
    display: flex;
    max-height: 260px;
    flex-direction: column;
    gap: 2px;
    overflow-y: auto;
  }

  .helix-header__country-list button {
    display: flex;
    width: 100%;
    justify-content: space-between;
    gap: 18px;
    padding: 8px 0;
    border: 0;
    background: transparent;
    font-size: 13px;
    text-align: left;
  }

  .helix-header__country-list button span:last-child {
    color: rgba(26, 19, 18, .55);
    white-space: nowrap;
  }

  .helix-header__country-list button:hover,
  .helix-header__country-list button:focus-visible {
    color: rgba(26, 19, 18, .55);
  }

  .helix-header__currency-panel {
    top: calc(100% + 32px);
    width: 330px;
    min-width: 330px;
    max-height: min(443px, calc(100vh - 130px));
    padding: 0;
    border: 0;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, .1);
    overflow: hidden;
  }

  .helix-header__currency-panel form,
  .helix-header__localization-sheet-inner form {
    display: flex;
    flex-direction: column;
    min-height: 0;
  }

  .helix-header__country-filter {
    position: relative;
    flex: 0 0 auto;
    padding: 16px 10px;
  }

  .helix-header__country-filter-input {
    display: block;
    width: 100%;
    height: 48px;
    min-height: 48px;
    padding: 0 52px 0 16px;
    border: 0;
    border-radius: 9990px;
    outline: 0;
    background: rgba(50, 50, 50, .06);
    color: #1a1312;
    font: inherit;
    font-size: 14px;
    line-height: 48px;
  }

  .helix-header__country-filter-input::placeholder {
    color: rgba(26, 19, 18, .62);
    opacity: 1;
  }

  .helix-header__country-filter-icon {
    position: absolute;
    top: 16px;
    right: 10px;
    display: grid;
    width: 48px;
    height: 48px;
    place-items: center;
    pointer-events: none;
  }

  .helix-header__country-filter-icon svg {
    width: 24px;
    height: 24px;
  }

  .helix-header__currency-panel .helix-header__country-list {
    flex: 1 1 auto;
    max-height: none;
    padding: 10px;
    overflow-y: auto;
  }

  .helix-header__country-list button {
    min-height: 42px;
    align-items: center;
    gap: 16px;
    padding: 10px 16px;
    border-radius: 9.6px;
    font-size: 14px;
    line-height: 22px;
  }

  .helix-header__country-option-name {
    display: flex;
    min-width: 0;
    flex: 1 1 auto;
    align-items: center;
    gap: 8px;
  }

  .helix-header__country-option-flag {
    display: inline-block;
    width: 16px;
    height: 16px;
    flex: 0 0 16px;
    border-radius: 50%;
    background: var(--helix-country-flag) center / contain no-repeat;
  }

  .helix-header__country-option-currency {
    flex: 0 0 auto;
    color: rgba(26, 19, 18, .55);
    white-space: nowrap;
  }

  .helix-header__country-list button[hidden] {
    display: none;
  }

  .helix-header__country-list:empty::after {
    padding: 12px 16px;
    color: rgba(26, 19, 18, .55);
    content: 'No results found';
    font-size: 13px;
  }

  .helix-header__scrim,
  .helix-header__mobile-menu,
  .helix-header__drawer {
    position: fixed;
    z-index: 50;
    inset: 0;
    visibility: hidden;
    opacity: 0;
    pointer-events: none;
    transition: opacity .3s ease, visibility 0s linear .3s;
  }

  .helix-header__scrim {
    width: 100%;
    padding: 0;
    border: 0;
    background: rgba(50, 50, 50, .5);
    -webkit-backdrop-filter: blur(20px);
    backdrop-filter: blur(20px);
  }

  .helix-header.is-mobile-menu-open .helix-header__scrim,
  .helix-header.is-search-open .helix-header__scrim,
  .helix-header.is-cart-open .helix-header__scrim,
  .helix-header.is-mobile-menu-open .helix-header__mobile-menu {
    visibility: visible;
    opacity: 1;
    pointer-events: auto;
    transition-delay: 0s;
  }

  .helix-header.is-search-open .helix-header__drawer--search,
  .helix-header.is-cart-open .helix-header__drawer--cart {
    visibility: visible;
    opacity: 1;
    transition-delay: 0s;
  }

  .helix-header__mobile-menu,
  .helix-header__drawer {
    display: flex;
    justify-content: flex-start;
  }

  .helix-header__drawer {
    justify-content: flex-end;
    padding: 16px;
    overflow: hidden;
  }

  .helix-header__mobile-menu-panel,
  .helix-header__drawer-panel {
    position: relative;
    display: flex;
    flex-direction: column;
    width: min(420px, 100%);
    height: 100%;
    overflow: hidden;
    padding: 24px clamp(20px, 5vw, 36px) 40px;
    background: #fff;
    color: #1a1312;
    box-shadow: 10px 0 40px rgba(26, 19, 18, .14);
    transform: translateX(-100%);
    transition: transform .5s cubic-bezier(.6, 0, .4, 1) .05s;
  }

  .helix-header__drawer-panel {
    width: min(550px, 100%);
    padding: 0;
    border-radius: 16px;
    box-shadow: none;
    overflow: hidden;
    transform: translateX(100%);
  }

  .helix-header.is-mobile-menu-open .helix-header__mobile-menu-panel,
  .helix-header.is-search-open .helix-header__drawer--search .helix-header__drawer-panel,
  .helix-header.is-cart-open .helix-header__drawer--cart .helix-header__drawer-panel {
    transform: translateX(0);
  }

  .helix-header.is-search-open .helix-header__drawer--search .helix-header__drawer-panel,
  .helix-header.is-cart-open .helix-header__drawer--cart .helix-header__drawer-panel {
    pointer-events: auto;
  }

  .helix-header__mobile-menu-top,
  .helix-header__drawer-top {
    display: flex;
    min-height: 40px;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 25px;
  }

  .helix-header__mobile-menu-top strong,
  .helix-header__drawer-top strong {
    font-size: 18px;
    font-weight: 600;
  }

  .helix-header__mobile-menu-logo {
    display: inline-flex;
    width: 90px;
    height: 16px;
    align-items: center;
    color: inherit;
    font-size: 0;
    line-height: 0;
  }

  .helix-header__mobile-menu-logo img {
    display: block;
    width: 100%;
    height: auto;
  }

  .helix-header__close-button {
    display: grid;
    width: 40px;
    height: 40px;
    padding: 0;
    place-items: center;
    border: 1px solid rgba(26, 19, 18, .18);
    border-radius: 50%;
    background: transparent;
    transition: transform .25s ease, background .25s ease;
  }

  .helix-header__close-button:hover,
  .helix-header__close-button:focus-visible {
    background: #f4f0ed;
    transform: rotate(90deg);
  }

  .helix-header__mobile-nav {
    position: relative;
    flex: 1 1 auto;
    min-height: 0;
    display: flex;
    flex-direction: column;
    overflow: hidden;
  }

  .helix-header__mobile-nav > a,
  .helix-header__mobile-nav > details > summary {
    display: flex;
    min-height: 54px;
    align-items: center;
    justify-content: space-between;
    gap: 18px;
    padding: 12px 0;
    border-bottom: 1px solid rgba(26, 19, 18, .14);
    font-size: 17px;
    font-weight: 500;
  }

  .helix-header__mobile-nav details > summary {
    list-style: none;
  }

  .helix-header__mobile-nav details > summary::-webkit-details-marker {
    display: none;
  }

  .helix-header__mobile-nav details > summary span:last-child {
    display: inline-flex;
    transform: rotate(-90deg);
    transition: transform .3s cubic-bezier(.6, .14, 0, 1);
  }

  .helix-header__mobile-submenu {
    position: absolute;
    z-index: 2;
    inset: 0;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    padding: 0;
    background: #fff;
    opacity: 1;
    pointer-events: none;
    transform: translateX(100%);
    visibility: hidden;
    transition: transform .3s ease-in-out, visibility 0s linear .3s;
  }

  .helix-header__mobile-nav details[open] > .helix-header__mobile-submenu {
    opacity: 1;
    pointer-events: auto;
    transform: translateX(0);
    visibility: visible;
    transition-delay: 0s;
  }

  @keyframes helix-mobile-slide-out {
    from {
      transform: translateX(0);
    }

    to {
      transform: translateX(100%);
    }
  }

  .helix-header__mobile-nav details[open] > .helix-header__mobile-submenu.is-mobile-submenu-closing {
    opacity: 1;
    pointer-events: none;
    transform: translateX(100%);
    visibility: visible;
    animation: helix-mobile-slide-out .3s ease-in-out both;
    transition: none;
  }

  .helix-header__mobile-submenu-header {
    display: flex;
    min-height: 56px;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    border-bottom: 1px solid rgba(26, 19, 18, .14);
  }

  .helix-header__mobile-back {
    display: inline-flex;
    min-height: 40px;
    min-width: 0;
    flex: 1;
    align-items: center;
    gap: 10px;
    padding: 0;
    border: 0;
    background: transparent;
    font-size: 18px;
    font-weight: 600;
    text-align: left;
  }

  .helix-header__mobile-back > span:first-child {
    display: inline-flex;
    transform: rotate(90deg);
  }

  .helix-header__mobile-submenu-inner {
    min-height: 0;
    flex: 1 1 auto;
    overflow-y: auto;
    padding: 16px 0 30px;
  }

  .helix-header__mobile-submenu-group {
    display: flex;
    flex-direction: column;
  }

  .helix-header__mobile-submenu-group + .helix-header__mobile-submenu-group {
    margin-top: 22px;
  }

  .helix-header__mobile-submenu-group a {
    display: flex;
    min-height: 48px;
    align-items: center;
    padding: 10px 0;
    border-bottom: 1px solid rgba(26, 19, 18, .12);
    font-size: 16px;
    line-height: 25.6px;
  }

  .helix-header__mobile-submenu-group--featured a {
    font-weight: 600;
  }

  .helix-header__mobile-submenu-details {
    border-bottom: 1px solid rgba(26, 19, 18, .12);
  }

  .helix-header__mobile-submenu-details > summary {
    display: flex;
    min-height: 56px;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    list-style: none;
    font-size: 16px;
  }

  .helix-header__mobile-submenu-details > summary::-webkit-details-marker {
    display: none;
  }

  .helix-header__mobile-submenu-details > summary span:last-child {
    display: inline-flex;
    transform: rotate(-90deg);
    transition: transform .3s cubic-bezier(.6, .14, 0, 1);
  }

  .helix-header__mobile-submenu-details[open] > summary span:last-child {
    transform: rotate(0deg);
  }

  .helix-header__mobile-submenu-details > .helix-header__mobile-submenu--child {
    z-index: 4;
  }

  .helix-header__mobile-submenu-details-content {
    display: flex;
    max-height: 0;
    flex-direction: column;
    overflow: hidden;
    opacity: 0;
    transition: max-height .35s cubic-bezier(.6, .14, 0, 1), opacity .2s ease, padding .35s cubic-bezier(.6, .14, 0, 1);
  }

  .helix-header__mobile-submenu-details[open] .helix-header__mobile-submenu-details-content {
    max-height: 360px;
    padding-bottom: 12px;
    opacity: 1;
  }

  .helix-header__mobile-submenu-label {
    margin: 0 0 4px;
    color: rgba(26, 19, 18, .58);
    font-size: 13px;
    font-weight: 600;
    line-height: 20.8px;
  }

  .helix-header__mobile-menu-footer {
    display: flex;
    flex: 0 0 auto;
    flex-direction: column;
    gap: 14px;
    padding-top: 14px;
    border-top: 1px solid rgba(26, 19, 18, .14);
  }

  .helix-header__mobile-socials {
    display: flex;
    justify-content: center;
    gap: 10px;
  }

  .helix-header__mobile-socials a {
    display: grid;
    width: 38px;
    height: 38px;
    place-items: center;
    border-radius: 50%;
    background: #edf0f2;
    color: #323232;
    transition: transform .25s ease, background .25s ease;
  }

  .helix-header__mobile-socials a:hover,
  .helix-header__mobile-socials a:focus-visible {
    background: #e3e7ea;
    transform: translateY(-2px);
  }

  .helix-header__mobile-socials svg {
    width: 18px;
    height: 18px;
  }

  .helix-header__mobile-login {
    display: flex;
    min-height: 48px;
    align-items: center;
    justify-content: center;
    border-radius: 9990px;
    background: #1a1312;
    color: #fff;
    font-size: 14px;
    font-weight: 500;
  }

  .helix-header__mobile-localization > summary {
    display: flex;
    min-height: 40px;
    align-items: center;
    gap: 8px;
    list-style: none;
    font-size: 14px;
  }

  .helix-header__mobile-localization > summary::-webkit-details-marker {
    display: none;
  }

  .helix-header__mobile-localization > summary span:last-child {
    display: inline-flex;
    margin-left: auto;
    transform: rotate(0deg);
    transition: transform .3s cubic-bezier(.6, .14, 0, 1);
  }

  .helix-header__mobile-localization[open] > summary span:last-child {
    transform: rotate(180deg);
  }

  .helix-header__mobile-localization-panel {
    display: flex;
    max-height: 0;
    flex-direction: column;
    gap: 2px;
    overflow: hidden;
    opacity: 0;
    transition: max-height .35s cubic-bezier(.6, .14, 0, 1), opacity .2s ease, padding .35s cubic-bezier(.6, .14, 0, 1);
  }

  .helix-header__mobile-localization[open] .helix-header__mobile-localization-panel {
    max-height: 260px;
    padding: 8px 0 10px;
    opacity: 1;
  }

  .helix-header__mobile-localization-panel button {
    display: flex;
    width: 100%;
    justify-content: space-between;
    padding: 8px 0;
    border: 0;
    background: transparent;
    font-size: 13px;
    text-align: left;
  }

  .helix-header__mobile-localization-panel button span {
    color: rgba(26, 19, 18, .55);
  }

  .helix-header__mobile-submenu a:hover,
  .helix-header__mobile-submenu a:focus-visible,
  .helix-header__mobile-localization-panel button:hover,
  .helix-header__mobile-localization-panel button:focus-visible {
    color: rgba(26, 19, 18, .55);
  }

  .helix-header__mobile-localization-row {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 20px;
  }

  .helix-header__mobile-localization-trigger {
    display: inline-flex;
    min-height: 30px;
    align-items: center;
    gap: 8px;
    padding: 0;
    border: 0;
    background: transparent;
    color: inherit;
    font: inherit;
    font-size: 14px;
    white-space: nowrap;
  }

  .helix-header__mobile-localization-trigger > span:last-child {
    display: inline-flex;
    width: 16px;
    height: 16px;
    transition: transform .3s cubic-bezier(.6, .14, 0, 1);
  }

  .helix-header__mobile-localization-trigger[aria-expanded='true'] > span:last-child {
    transform: rotate(180deg);
  }

  .helix-header__localization-sheet {
    position: fixed;
    z-index: 60;
    inset: 0;
    visibility: hidden;
    opacity: 0;
    pointer-events: none;
    transition: opacity .1s ease-in-out, visibility 0s linear .1s;
  }

  .helix-header__localization-sheet.is-open {
    visibility: visible;
    opacity: 1;
    pointer-events: auto;
    transition-delay: 0s;
  }

  .helix-header__localization-sheet-overlay {
    position: absolute;
    inset: 0;
    width: 100%;
    padding: 0;
    border: 0;
    background: rgba(50, 50, 50, .3);
    backdrop-filter: blur(20px);
  }

  .helix-header__localization-sheet-container {
    position: absolute;
    right: 0;
    bottom: 0;
    left: 0;
    display: flex;
    max-height: min(570px, calc(100% - 24px));
    flex-direction: column;
    overflow: hidden;
    border-radius: 16px 16px 0 0;
    background: #fff;
    color: #1a1312;
    transform: translateY(100%);
    transition: transform .4s cubic-bezier(.6, 0, .4, 1);
  }

  .helix-header__localization-sheet.is-open .helix-header__localization-sheet-container {
    transform: translateY(0);
  }

  .helix-header__localization-sheet--language .helix-header__localization-sheet-container {
    max-height: 154px;
  }

  .helix-header__localization-sheet-handle {
    position: absolute;
    z-index: 2;
    top: 12px;
    left: 50%;
    width: 35px;
    height: 4px;
    border-radius: 99px;
    background: #d1d1d1;
    transform: translateX(-50%);
  }

  .helix-header__localization-sheet-header {
    position: relative;
    z-index: 1;
    display: flex;
    min-height: 77px;
    flex: 0 0 auto;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    padding: 24px 16px 16px;
    border-bottom: 1px solid rgba(26, 19, 18, .14);
    background: #fff;
  }

  .helix-header__localization-sheet-header--country {
    min-height: 60px;
    padding-bottom: 0;
    border-bottom: 0;
  }

  .helix-header__localization-sheet-header h2 {
    margin: 0;
    flex: 1 1 auto;
    color: #131313;
    font-family: 'Archivo', var(--helix-font, sans-serif);
    font-size: 17px;
    font-weight: 700;
    letter-spacing: .34px;
    line-height: 23.8px;
  }

  .helix-header__localization-close-button {
    display: grid;
    width: 36px;
    height: 36px;
    flex: 0 0 36px;
    padding: 0;
    place-items: center;
    border: 0;
    border-radius: 99px;
    background: #fff;
    color: #131313;
    transition: background .3s ease-in-out, color .3s ease-in-out;
  }

  .helix-header__localization-close-button:hover,
  .helix-header__localization-close-button:focus-visible {
    background: #f4f0ed;
  }

  .helix-header__localization-close-button svg {
    width: 20px;
    height: 20px;
  }

  .helix-header__localization-sheet-inner {
    min-height: 0;
    flex: 1 1 auto;
    overflow: hidden;
  }

  .helix-header__localization-sheet-inner--language {
    overflow: visible;
  }

  .helix-header__localization-sheet .helix-header__country-filter {
    padding: 20px 10px 16px;
  }

  .helix-header__localization-sheet .helix-header__country-filter-input {
    height: 44px;
    min-height: 44px;
    padding-inline: 15px 52px;
    line-height: 44px;
  }

  .helix-header__localization-sheet .helix-header__country-filter-icon {
    top: 18px;
  }

  .helix-header__localization-sheet .helix-header__country-list {
    max-height: none;
    padding: 20px 10px;
    overflow-y: auto;
  }

  .helix-header__localization-sheet .helix-header__country-list button {
    min-height: 37px;
    padding: 10px 16px;
    border-radius: 9.6px;
    font-size: 14px;
    line-height: 17px;
  }

  .helix-header__language-list {
    display: flex;
    flex-direction: column;
    gap: 2px;
    padding: 20px 10px;
    overflow-y: auto;
  }

  .helix-header__language-list button {
    display: flex;
    width: 100%;
    min-height: 37px;
    align-items: center;
    padding: 10px 16px;
    border: 0;
    border-radius: 9.6px;
    background: rgba(50, 50, 50, .06);
    color: #323232;
    font: inherit;
    font-size: 14px;
    line-height: normal;
    text-align: left;
  }

  .helix-header__language-list button:hover,
  .helix-header__language-list button:focus-visible {
    color: rgba(26, 19, 18, .55);
  }

  .helix-header__mobile-submenu a,
  .helix-header__mobile-submenu button {
    text-align: left;
  }

  .helix-header__mobile-submenu button {
    padding: 0;
    border: 0;
    background: transparent;
  }

  .helix-header__mobile-submenu button span {
    color: inherit;
  }

  .helix-header__mobile-submenu button:hover,
  .helix-header__mobile-submenu button:focus-visible {
    color: rgba(26, 19, 18, .55);
  }

  .helix-header__mobile-nav details[open] > summary span:last-child {
    transform: rotate(-90deg);
  }

  .helix-header__mobile-nav details[open] .helix-header__mobile-submenu {
    opacity: 1;
  }

  .helix-header__drawer-top {
    flex: 0 0 73px;
    min-height: 73px;
    margin: 0;
    padding: 16px 30px;
  }

  .helix-header__drawer-top strong {
    color: #131313;
    font-family: 'Archivo', sans-serif;
    font-size: 18px;
    font-weight: 700;
    letter-spacing: .36px;
    line-height: 18px;
  }

  .helix-header__drawer--search .helix-header__drawer-top strong {
    line-height: 25.2px;
  }

  .helix-header__drawer .helix-header__close-button {
    width: 40px;
    height: 40px;
    border: 0;
    background: #fff;
  }

  .helix-header__drawer-inner {
    flex: 1 1 auto;
    min-height: 0;
    overflow-y: auto;
  }

  .helix-header__drawer-inner--search {
    padding: 30px 36px 30px 30px;
  }

  .helix-header__drawer-inner--cart {
    padding: 40px 36px 40px 30px;
  }

  .helix-header__search-form {
    position: relative;
    display: block;
  }

  .helix-header__search-form input {
    width: 100%;
    height: 48px;
    min-height: 48px;
    padding: 0 60px 0 16px;
    border: 1px solid #131313;
    border-radius: 9999px;
    outline: 0;
    background: rgba(50, 50, 50, .06);
    font-size: 14px;
    line-height: 48px;
  }

  .helix-header__search-form button {
    position: absolute;
    top: 2px;
    right: 0;
    display: grid;
    width: 44px;
    height: 44px;
    padding: 0;
    place-items: center;
    border: 0;
    background: transparent;
  }

  .helix-header__search-popular {
    margin-top: 30px;
  }

  .helix-header__search-popular h3 {
    margin: 0 0 20px;
    color: #131313;
    font-family: 'Archivo', sans-serif;
    font-size: 18px;
    font-weight: 700;
    letter-spacing: .36px;
    line-height: 25.2px;
  }

  .helix-header__search-links {
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
  }

  .helix-header__search-links a {
    display: inline-flex;
    min-height: 34px;
    align-items: center;
    justify-content: center;
    padding: 6px 12px;
    border: 0;
    border-radius: 9999px;
    background: rgba(50, 50, 50, .06);
    font-size: 14px;
    font-weight: 400;
    line-height: 14px;
    transition: background .25s ease, color .25s ease;
  }

  .helix-header__search-links a:hover,
  .helix-header__search-links a:focus-visible {
    background: rgba(50, 50, 50, .12);
  }

  .helix-header__cart-summary,
  .helix-header__cart-empty {
    text-align: center;
  }

  .helix-header__cart-empty h3 {
    margin: 0;
    color: #131313;
    font-family: 'Archivo', sans-serif;
    font-size: 24px;
    font-weight: 700;
    letter-spacing: .48px;
    line-height: 31.2px;
  }

  .helix-header__cart-empty p {
    margin: 12px 0 0;
    font-size: 14px;
    line-height: 22.4px;
  }

  .helix-header__cart-collections {
    display: flex;
    flex-direction: column;
    gap: 12px;
    margin-top: 32px;
  }

  .helix-header__drawer-button {
    display: inline-flex;
    width: 100%;
    min-height: 48px;
    align-items: center;
    justify-content: center;
    padding: 6px 22px;
    border: 0;
    border-radius: 9999px;
    background: #ececec;
    font-size: 14px;
    font-weight: 500;
    letter-spacing: .28px;
    line-height: 14px;
    transition: background .25s ease, color .25s ease;
  }

  .helix-header__drawer-button:hover,
  .helix-header__drawer-button:focus-visible {
    background: #dedede;
  }

  .helix-header__cart-message {
    margin: 0 0 10px;
    font-size: 16px;
  }

  .helix-header__cart-total {
    margin-bottom: 26px;
    font-size: 22px;
    font-weight: 600;
  }

  .helix-header__drawer-button--dark {
    border-color: #1a1312;
    background: #1a1312;
    color: #fff;
  }

  .helix-header__mobile-bar {
    display: none;
  }

  .helix-header--overlay .helix-header__desktop-bar,
  .helix-header--overlay .helix-header__mobile-bar {
    color: var(--helix-overlay-text);
  }

  .helix-header--overlay .helix-header__logo--normal,
  .helix-header--overlay .helix-header__logo-text--normal,
  .helix-header--overlay .helix-header__logo--overlay,
  .helix-header--overlay .helix-header__logo-text--overlay {
    display: none;
  }

  .helix-header--overlay .helix-header__logo--overlay,
  .helix-header--overlay .helix-header__logo-text--overlay {
    display: block;
  }

  .helix-header.is-menu-open .helix-header__desktop-bar,
  .helix-header.is-menu-open .helix-header__mobile-bar {
    color: var(--helix-solid-text);
  }

  .helix-header.is-menu-open .helix-header__desktop-surface {
    background: var(--helix-header-bg);
    box-shadow: 0 1px 0 rgba(26, 19, 18, .12);
    color: var(--helix-solid-text);
  }

  .helix-header.is-menu-open .helix-header__menu-background {
    height: calc(80px + var(--helix-menu-height, 0px));
    background: var(--helix-header-bg);
  }

  .helix-header.is-replaying-menu-reveal .helix-header__mega-panel {
    height: 0;
    opacity: 0;
    pointer-events: none;
    transition: none;
    visibility: hidden;
  }

  .helix-header.is-menu-open .helix-header__logo--normal,
  .helix-header.is-menu-open .helix-header__logo-text--normal {
    display: block;
  }

  .helix-header.is-menu-open .helix-header__logo--overlay,
  .helix-header.is-menu-open .helix-header__logo-text--overlay {
    display: none;
  }

  .helix-header.is-scrolled .helix-header__desktop-bar,
  .helix-header.is-scrolled .helix-header__mobile-bar {
    position: fixed;
    top: 0;
  }

  .helix-header:not(.helix-header--overlay).is-scrolled .helix-header__desktop-surface,
  .helix-header:not(.helix-header--overlay).is-scrolled .helix-header__mobile-inner {
    background: var(--helix-header-bg);
    box-shadow: 0 1px 0 rgba(26, 19, 18, .12);
    color: var(--helix-solid-text);
    backdrop-filter: blur(12px);
  }

  .helix-header:not(.helix-header--overlay).is-scrolled .helix-header__logo--normal,
  .helix-header:not(.helix-header--overlay).is-scrolled .helix-header__logo-text--normal {
    display: block;
  }

  .helix-header:not(.helix-header--overlay).is-scrolled .helix-header__logo--overlay,
  .helix-header:not(.helix-header--overlay).is-scrolled .helix-header__logo-text--overlay {
    display: none;
  }

  @media (min-width: 991px) {
    .helix-header--overlay.is-scrolled .helix-header__desktop-bar {
      color: var(--helix-solid-text);
    }

    .helix-header--overlay.is-scrolled .helix-header__desktop-surface {
      width: calc(100% - (var(--helix-desktop-side-padding) * 2));
      margin-inline: var(--helix-desktop-side-padding);
      border-radius: 16px;
      background: var(--helix-header-bg);
      box-shadow: none;
      color: var(--helix-solid-text);
      transform: translateY(16px);
      transition: background-color .15s cubic-bezier(.6, 0, .4, 1), border-radius .15s cubic-bezier(.6, .14, 0, 1), color .15s cubic-bezier(.6, 0, .4, 1), transform .5s cubic-bezier(.625, .05, 0, 1);
    }

    .helix-header--overlay.is-scrolled .helix-header__desktop-inner {
      width: calc(100% - 60px);
      margin-inline: 30px;
    }

    .helix-header--overlay.is-scrolled .helix-header__menu-background {
      top: 16px;
      right: var(--helix-desktop-side-padding);
      left: var(--helix-desktop-side-padding);
      border-radius: 16px;
    }

    .helix-header--overlay.is-scrolled .helix-header__mega-panel {
      right: -30px;
      left: -30px;
      border-radius: 0 0 16px 16px;
    }

    .helix-header--overlay.is-scrolled .helix-header__mega-columns {
      padding-inline: 30px;
    }

    .helix-header--overlay.is-scrolled .helix-header__logo--normal,
    .helix-header--overlay.is-scrolled .helix-header__logo-text--normal {
      display: block;
    }

    .helix-header--overlay.is-scrolled .helix-header__logo--overlay,
    .helix-header--overlay.is-scrolled .helix-header__logo-text--overlay {
      display: none;
    }

    .helix-header__search-form input {
      border-color: transparent;
    }

    .helix-header__drawer-button {
      padding-inline: 24px;
    }
  }

  @media (min-width: 991px) and (max-width: 1199px) {
    .helix-header--overlay.is-scrolled .helix-header__desktop-surface {
      width: calc(100% - 60px);
      margin-inline: 30px;
    }

    .helix-header--overlay.is-scrolled .helix-header__menu-background {
      right: 30px;
      left: 30px;
    }
  }

  .helix-header__logo-text {
    text-transform: uppercase;
  }

  .helix-header__mobile-icon {
    display: grid;
    width: 40px;
    height: 40px;
    padding: 0;
    place-items: center;
    border: 0;
    background: transparent;
    transition: opacity .25s ease, transform .25s ease;
  }

  .helix-header__mobile-icon:hover,
  .helix-header__mobile-icon:focus-visible {
    opacity: .62;
    transform: translateY(-1px);
  }

  .helix-header__mobile-icon svg {
    width: 24px;
    height: 24px;
  }

  @keyframes helix-menu-reveal {
    from { opacity: 0; transform: translate3d(0, 15px, 0); }
    to { opacity: 1; transform: translate3d(0, 0, 0); }
  }

  @keyframes helix-menu-link-reveal {
    from { opacity: 0; transform: translate3d(15px, 0, 0); }
    to { opacity: 1; transform: translate3d(0, 0, 0); }
  }

  @media (max-width: 990px) {
    .helix-header__desktop-bar {
      display: none;
    }

    .helix-header__mega-scrim {
      display: none;
    }

    .helix-header__drawer {
      padding: 8px;
    }

    .helix-header__drawer-panel {
      width: 100%;
      border-radius: 16px;
    }

    .helix-header__drawer-top {
      flex-basis: 69px;
      min-height: 69px;
      padding: 16px;
    }

    .helix-header__drawer-top strong {
      font-size: 17px;
      letter-spacing: .34px;
      line-height: 23.8px;
    }

    .helix-header__drawer--cart .helix-header__drawer-top strong {
      line-height: 17px;
    }

    .helix-header__drawer .helix-header__close-button {
      width: 36px;
      height: 36px;
    }

    .helix-header__drawer-inner--search {
      padding: 30px 16px;
    }

    .helix-header__drawer-inner--cart {
      padding: 40px 16px;
    }

    .helix-header__search-form input {
      height: 44px;
      min-height: 44px;
      line-height: 44px;
    }

    .helix-header__search-form button {
      top: 0;
      height: 44px;
    }

    .helix-header__search-popular h3 {
      font-size: 17px;
      letter-spacing: .34px;
      line-height: 23.8px;
    }

    .helix-header__cart-empty h3 {
      font-size: 22px;
      letter-spacing: .44px;
      line-height: 28.6px;
    }

    .helix-header__drawer-button {
      min-height: 44px;
    }

    .helix-header__mobile-menu {
      inset: 8px;
      overflow: hidden;
      border-radius: 16px;
    }

    .helix-header__mobile-menu-panel {
      width: 100%;
      height: 100%;
      padding: 0;
      border-radius: inherit;
      box-shadow: none;
    }

    .helix-header__mobile-menu-top {
      min-height: 69px;
      margin: 0;
      padding: 16px;
      border-bottom: 1px solid rgba(26, 19, 18, .14);
    }

    .helix-header__mobile-menu-top .helix-header__close-button {
      width: 36px;
      height: 36px;
      border: 0;
    }

    .helix-header__mobile-submenu-header .helix-header__close-button {
      width: 36px;
      height: 36px;
      border: 0;
    }

    .helix-header__mobile-submenu--child .helix-header__close-button {
      display: none;
    }

    .helix-header__mobile-nav {
      position: static;
      flex: 0 0 auto;
      overflow: visible;
      padding: 20px 16px 0;
    }

    .helix-header__mobile-nav > details {
      border-bottom: 1px solid rgba(26, 19, 18, .14);
    }

    .helix-header__mobile-nav > a {
      box-sizing: border-box;
      min-height: 58.6px;
      padding: 16px 0;
      font-size: 16px;
      letter-spacing: .32px;
      border-bottom: 1px solid rgba(26, 19, 18, .14);
    }

    .helix-header__mobile-nav > details > summary {
      box-sizing: border-box;
      min-height: 57.6px;
      padding: 16px 0;
      border-bottom: 0;
      font-size: 16px;
      letter-spacing: .32px;
    }

    .helix-header__mobile-nav-summary {
      gap: 0 !important;
      justify-content: flex-start !important;
    }

    .helix-header__mobile-title-link {
      display: block;
      width: 50%;
      margin: -10px 0;
      padding: 10px 0;
    }

    .helix-header__mobile-summary-label {
      pointer-events: none;
    }

    .helix-header__mobile-submenu {
      z-index: 3;
    }

    .helix-header__mobile-submenu-header {
      box-sizing: border-box;
      height: 69px;
      min-height: 69px;
      flex: 0 0 auto;
      gap: 8px;
      padding: 16px 16px 16px 12px;
    }

    .helix-header__mobile-submenu-header .helix-header__mobile-back {
      height: 18px;
      min-height: 18px;
      gap: 8px;
      font-family: 'Figtree', var(--helix-font, sans-serif);
      font-size: 14px;
      font-weight: 400;
      letter-spacing: normal;
      line-height: 22.4px;
    }

    .helix-header__mobile-submenu-header .helix-header__mobile-back > span:first-child {
      width: 18px;
      height: 18px;
      flex: 0 0 18px;
    }

    .helix-header__mobile-submenu-header .helix-header__mobile-back > span:first-child svg {
      width: 18px;
      height: 18px;
    }

    .helix-header__mobile-submenu-header .helix-header__mobile-back > span:first-child svg path {
      stroke-width: 1.4px;
    }

    .helix-header__mobile-submenu-header .helix-header__mobile-back > span:last-child {
      font-family: 'Archivo', var(--helix-font, sans-serif);
      font-size: 17px;
      font-weight: 700;
      letter-spacing: .34px;
      line-height: 17px;
    }

    .helix-header__mobile-menu .helix-header__close-button svg {
      width: 20px;
      height: 20px;
    }

    .helix-header__mobile-menu .helix-header__close-button svg path {
      stroke-width: 1px;
    }

    .helix-header__mobile-submenu-inner {
      padding: 20px 16px 30px;
      scrollbar-width: none;
      -ms-overflow-style: none;
    }

    .helix-header__mobile-submenu--mega > .helix-header__mobile-submenu-inner {
      display: flex;
      flex-direction: column;
      gap: 16px;
      padding: 20px 16px;
    }

    .helix-header__mobile-submenu-list {
      display: flex;
      flex-direction: column;
      gap: 0;
    }

    .helix-header__mobile-submenu-inner::-webkit-scrollbar {
      width: 0;
      height: 0;
      display: none;
    }

    .helix-header__mobile-submenu-group--featured {
      gap: 8px;
      margin-bottom: 0;
    }

    .helix-header__mobile-submenu-group--featured a {
      height: 25.6px;
      min-height: 0;
      padding: 0;
      border-bottom: 0;
      color: #131313;
      font-family: 'Archivo', var(--helix-font, sans-serif);
      font-size: 16px;
      font-weight: 700;
      line-height: 25.6px;
    }

    .helix-header__mobile-submenu--mega .helix-header__mobile-submenu--child .helix-header__mobile-submenu-group {
      gap: 16px;
    }

    .helix-header__mobile-submenu--mega .helix-header__mobile-submenu--child .helix-header__mobile-submenu-group a {
      height: 25.6px;
      min-height: 0;
      padding: 0;
      border-bottom: 0;
      color: #323232;
    }

    .helix-header__mobile-submenu-group--pages a {
      color: #323232;
      font-family: 'Figtree', var(--helix-font, sans-serif);
      font-weight: 400;
      letter-spacing: normal;
    }

    .helix-header__mobile-submenu-group--pages {
      gap: 16px;
    }

    .helix-header__mobile-submenu-details > summary {
      min-height: 57.6px;
      font-weight: 500;
      gap: normal;
      letter-spacing: .32px;
      color: #131313;
    }

    .helix-header__mobile-submenu-details > summary > span:last-child {
      display: inline-flex;
      width: 18px;
      height: 18px;
      flex: 0 0 18px;
      align-items: center;
      justify-content: center;
    }

    .helix-header__mobile-submenu-details > summary > span:last-child svg {
      width: 18px;
      height: 18px;
    }

    .helix-header__mobile-submenu-details > summary > span:last-child svg path {
      stroke-width: 1.4px;
    }

    .helix-header__mobile-nav > details > summary > span:last-child,
    .helix-header__mobile-submenu-details > summary > span:last-child {
      display: inline-flex;
      width: 18px;
      height: 18px;
      flex: 0 0 18px;
      align-items: center;
      justify-content: center;
      transform: none !important;
    }

    .helix-header__mobile-nav > details > summary > span:last-child svg,
    .helix-header__mobile-submenu-details > summary > span:last-child svg {
      width: 18px;
      height: 18px;
      transform: none !important;
    }

    .helix-header__mobile-nav > details > summary > span:last-child svg path,
    .helix-header__mobile-submenu-details > summary > span:last-child svg path {
      stroke-width: 1.4px;
    }

    .helix-header__mobile-submenu-header .helix-header__mobile-back > span:first-child {
      transform: none;
    }

    .helix-header__mobile-submenu-header .helix-header__mobile-back > span:first-child svg {
      transform: rotate(180deg) !important;
    }

    .helix-header__mobile-nav > details > .helix-header__mobile-nav-summary--split > .helix-header__mobile-toggle,
    .helix-header__mobile-nav > details > .helix-header__mobile-nav-summary:not(.helix-header__mobile-nav-summary--split) > .helix-header__mobile-toggle {
      display: flex;
      width: auto;
      height: 45.6px;
      flex: 1 1 0;
      align-items: center;
      justify-content: flex-end;
      margin: -10px 0;
      padding: 10px 0;
    }

    .helix-header__mobile-promos {
      display: flex;
      flex-direction: column;
      gap: 20px;
      margin-top: 0;
      padding-top: 20px;
    }

    .helix-header__mobile-promo {
      display: grid;
      gap: 0;
      color: #323232;
    }

    .helix-header__mobile-promo-media {
      display: block;
      position: relative;
      z-index: 3;
      overflow: hidden;
      height: auto !important;
      max-height: none !important;
      aspect-ratio: auto !important;
      border-radius: 16px;
      background: #f1f3f4;
    }

    .helix-header__mobile-promo-media img {
      display: block;
      width: 100%;
      min-height: 0;
      height: auto !important;
      max-height: none !important;
      object-fit: cover;
    }

    .helix-header__mobile-promo-details {
      box-sizing: border-box;
      position: relative;
      z-index: 2;
      height: 117px;
      min-height: 117px;
      margin-top: -40px;
      padding: 40px 0 0;
      background: #fff;
    }

    .helix-header__mobile-promo-details-inner {
      box-sizing: border-box;
      display: flex;
      height: 77px;
      flex-direction: column;
      padding: 16px 0 0;
    }

    .helix-header__mobile-promo strong {
      width: fit-content;
      color: #131313;
      font-family: 'Archivo', var(--helix-font, sans-serif);
      font-size: 16px;
      font-weight: 700;
      letter-spacing: .32px;
      line-height: 24px;
    }

    .helix-header__mobile-promo-details-inner > span {
      display: inline-flex;
      width: fit-content;
      gap: 8px;
      height: 29px;
      box-sizing: border-box;
      margin-top: 8px;
      padding: 3px 0;
      color: #1e1e1e;
      font-family: 'Figtree', var(--helix-font, sans-serif);
      font-size: 14px;
      font-weight: 500;
      letter-spacing: .28px;
      line-height: 23px;
      align-items: center;
    }

    .helix-header__mobile-promo-cta-text {
      display: flex;
      height: 23px;
      padding: 3px 0;
    }

    .helix-header__mobile-promo-cta-icon {
      display: inline-flex;
      width: 16px;
      height: 16px;
      flex: 0 0 16px;
      align-items: center;
      justify-content: center;
    }

    .helix-header__mobile-promo-cta-icon svg {
      width: 16px;
      height: 16px;
    }

    .helix-header__mobile-products {
      margin-top: 10px;
    }

    .helix-header__mobile-products-title {
      margin: 0 0 24px;
      color: #323232;
      font-family: 'Archivo', var(--helix-font, sans-serif);
      font-size: 17px;
      font-weight: 700;
      letter-spacing: .34px;
      line-height: 23.8px;
    }

    .helix-header__mobile-products-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 28px 8px;
    }

    .helix-header__mobile-products .helix-header__mobile-product {
      width: auto;
      min-width: 0;
      opacity: 1;
      transform: none;
    }

    .helix-header__mobile-products .helix-header__mega-product-media {
      background: #f1f3f4;
    }

    .helix-header__mobile-products .helix-header__mega-product-quick-view {
      display: none;
    }

    .helix-header__mobile-products .helix-header__mega-product-quick-view svg {
      width: 16px;
      height: 16px;
    }

    .helix-header__mobile-products .helix-header__mega-product-quick-add {
      display: none;
    }

    .helix-header__mobile-products .helix-header__mega-product-details {
      min-height: 0;
      gap: 6px;
      padding-top: 12px;
    }

    .helix-header__mobile-products .helix-header__mega-product-swatches {
      width: 68px;
      min-width: 68px;
      height: 35px;
      gap: 8px;
    }

    .helix-header__mobile-products .helix-header__mega-product-swatches li {
      width: 30px;
      height: 35px;
      padding-bottom: 5px;
    }

    .helix-header__mobile-products .helix-header__mega-product-swatches li a,
    .helix-header__mobile-products .helix-header__mega-product-swatches li span {
      width: 30px;
      height: 30px;
    }

    .helix-header__mobile-products .helix-header__mega-product-info {
      width: auto;
      max-width: 100%;
      align-self: flex-start;
      font-size: 14px;
      line-height: 22.4px;
    }

    .helix-header__mobile-products .helix-header__mega-product-info > a {
      display: block;
      max-height: 44.8px;
      min-height: 44.8px;
      overflow: hidden;
      font-weight: 400;
      overflow-wrap: anywhere;
      white-space: normal;
    }

    .helix-header__mobile-menu-footer {
      gap: 14px;
      margin-top: auto;
      padding: 14px 0;
      border-top: 0;
    }

    .helix-header__mobile-account-row {
      box-sizing: border-box;
      width: 100%;
      padding: 0 16px 14px;
      border-bottom: 1px solid rgba(26, 19, 18, .14);
    }

    .helix-header__mobile-login {
      width: 100%;
      min-height: 44px;
      padding: 6px 22px;
      background: #24272a;
      color: #fff !important;
      font-weight: 500;
      letter-spacing: .28px;
      line-height: normal;
    }

    .helix-header__mobile-localization-row {
      box-sizing: border-box;
      width: 100%;
      min-height: 38.4px;
      align-items: center;
      gap: 20px;
      padding: 0 16px 14px;
      border-bottom: 1px solid rgba(26, 19, 18, .14);
    }

    .helix-header__mobile-localization-trigger {
      box-sizing: border-box;
      min-height: 22.4px;
      height: 22.4px;
      line-height: 22.4px;
    }

    .helix-header__mobile-socials {
      box-sizing: border-box;
      width: 100%;
      min-height: 38px;
      padding: 0 16px;
    }

    .helix-header__mobile-bar {
      display: block;
      min-height: 56px;
      transition: transform .5s cubic-bezier(.6, 0, .4, 1) .2s;
    }

    .helix-header__mobile-inner {
      display: grid;
      min-height: 56px;
      grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
      align-items: center;
      padding-inline: 16px;
    }

    .helix-header__mobile-side {
      display: flex;
      align-items: center;
    }

    .helix-header__mobile-side--left {
      margin-left: -8px;
    }

    .helix-header__mobile-side--right {
      justify-content: flex-end;
      gap: 0;
      margin-right: -8px;
    }

    .helix-header__brand--mobile {
      justify-self: center;
    }

    .helix-header__brand--mobile .helix-header__logo-text {
      font-size: 16px;
    }

    .helix-header__mobile-side--right .helix-header__mobile-icon {
      width: 40px;
      height: 40px;
    }

    .helix-header.is-scroll-hidden .helix-header__mobile-bar {
      transform: translateY(-110%);
    }

    .helix-header--overlay.is-scrolled .helix-header__mobile-bar {
      color: var(--helix-solid-text);
    }

    .helix-header--overlay.is-scrolled .helix-header__mobile-inner {
      background: var(--helix-header-bg);
      box-shadow: none;
      color: var(--helix-solid-text);
    }

    .helix-header--overlay.is-scrolled .helix-header__logo--normal,
    .helix-header--overlay.is-scrolled .helix-header__logo-text--normal {
      display: block;
    }

    .helix-header--overlay.is-scrolled .helix-header__logo--overlay,
    .helix-header--overlay.is-scrolled .helix-header__logo-text--overlay {
      display: none;
    }

    .helix-header__mobile-submenu a:hover,
    .helix-header__mobile-submenu a:focus-visible,
    .helix-header__mobile-submenu button:hover,
    .helix-header__mobile-submenu button:focus-visible {
      color: inherit;
    }

    .helix-header__mobile-submenu-group--featured a:hover,
    .helix-header__mobile-submenu-group--featured a:focus-visible {
      color: #131313;
    }

    .helix-header__mobile-submenu-group--pages a:hover,
    .helix-header__mobile-submenu-group--pages a:focus-visible,
    .helix-header__mobile-submenu--child .helix-header__mobile-submenu-group a:hover,
    .helix-header__mobile-submenu--child .helix-header__mobile-submenu-group a:focus-visible {
      color: #323232;
    }

    .helix-header__announcement {
      padding-inline: 8px;
    }

    .helix-header__announcement-track {
      min-width: 0;
      width: min(330px, calc(100vw - 72px));
    }

    .helix-header__announcement-message {
      font-size: 12px;
      line-height: 18px;
      white-space: nowrap;
    }

    .helix-header__announcement-side button {
      width: 28px;
      height: 28px;
      font-size: 16px;
    }

    .helix-header__mega-inner {
      padding-inline: 20px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .helix-header *,
    .helix-header *::before,
    .helix-header *::after {
      animation-duration: .01ms !important;
      animation-iteration-count: 1 !important;
      scroll-behavior: auto !important;
      transition-duration: .01ms !important;
    }
  }

  @media (max-width: 520px) {
    .helix-header__announcement-message {
      font-size: 11px;
    }

    .helix-header__mobile-submenu {
      grid-template-columns: 1fr;
    }
  }
{% endstylesheet %}

{% javascript %}
  (() => {
    const initHelixHeader = (root) => {
      if (!root || root.dataset.helixReady === 'true') return;
      root.dataset.helixReady = 'true';

    const select = (selector, scope = root) => scope.querySelector(selector);
    const selectAll = (selector, scope = root) => Array.from(scope.querySelectorAll(selector));
    const desktopQuery = window.matchMedia('(min-width: 991px)');
    const dropdowns = selectAll('[data-helix-dropdown]');
    const mobileMenu = select('[data-helix-mobile-menu]');
    const searchDrawer = select('[data-helix-search-drawer]');
    const cartDrawer = select('[data-helix-cart-drawer]');
    const megaScrim = select('[data-helix-mega-scrim]');
    const mobileMenuPanel = select('.helix-header__mobile-menu-panel');
    const mobileSections = selectAll('[data-helix-mobile-section]');
    const mobileDetails = selectAll('.helix-header__mobile-nav details');
    const mobileSubmenuSections = selectAll('.helix-header__mobile-submenu-details');
    const localizationSheets = selectAll('[data-helix-localization-sheet]');
    const localizationTriggers = selectAll('[data-helix-open-localization]');
    const countryFilters = selectAll('[data-helix-country-filter]');
    const searchPanel = select('.helix-header__drawer--search .helix-header__drawer-panel');
    const cartPanel = select('.helix-header__drawer--cart .helix-header__drawer-panel');
    let announcementIndex = 0;
    let announcementTimer;
    let closeDropdownTimer;
    let lastScrollY = window.scrollY;
    const menProducts = select('[data-helix-men-products]');
    const menProductsViewport = select('[data-helix-men-products-viewport]', menProducts);
    const menProductsTrack = select('[data-helix-men-products-track]', menProducts);
    const menProductsPrev = select('[data-helix-men-products-prev]', menProducts);
    const menProductsNext = select('[data-helix-men-products-next]', menProducts);
    let menProductsOffset = 0;

    const syncMobileSubmenuState = () => {
      const hasOpenSubmenu = mobileSections.some((section) => section.open && section.dataset.helixMobileClosing !== 'true');
      mobileMenuPanel?.classList.toggle('is-mobile-submenu-open', hasOpenSubmenu);
    };

    const getMobileSubmenuPanel = (section) => section?.querySelector(':scope > .helix-header__mobile-submenu');

    const resetMobileSubmenuTransition = (section) => {
      getMobileSubmenuPanel(section)?.classList.remove('is-mobile-submenu-closing');
      section?.removeAttribute('data-helix-mobile-closing');
    };

    const closeMobileSubmenu = (section) => {
      if (!section?.open) return;

      const panel = getMobileSubmenuPanel(section);
      if (!panel) {
        section.removeAttribute('open');
        syncMobileSubmenuState();
        return;
      }

      if (section.dataset.helixMobileClosing === 'true') return;

      section.dataset.helixMobileClosing = 'true';
      panel.classList.add('is-mobile-submenu-closing');
      syncMobileSubmenuState();

      let fallbackTimer;
      let finished = false;
      const finish = () => {
        if (finished) return;
        finished = true;
        panel.removeEventListener('animationend', handleAnimationEnd);
        window.clearTimeout(fallbackTimer);
        panel.classList.remove('is-mobile-submenu-closing');
        section.removeAttribute('data-helix-mobile-closing');
        section.removeAttribute('open');
        syncMobileSubmenuState();
      };
      const handleAnimationEnd = (event) => {
        if (event.animationName === 'helix-mobile-slide-out') finish();
      };

      panel.addEventListener('animationend', handleAnimationEnd);
      fallbackTimer = window.setTimeout(finish, 360);
    };

    const normalizeAnnouncementMessage = () => {
      const firstMessage = select('[data-helix-announcement-message]');
      if (!firstMessage) return;
      const messageText = firstMessage.textContent.replace(/\\s+/g, ' ').trim();
      if (!messageText.includes('Free shipping for all orders over') || !messageText.includes('5.000.000₫Free shipping for all orders over')) return;
      firstMessage.innerHTML = 'Free shipping for all orders over <strong>5.000.000₫</strong>';
    };

    normalizeAnnouncementMessage();

    const syncMenProducts = () => {
      if (!menProductsViewport || !menProductsTrack) return;
      const maxOffset = Math.max(0, menProductsTrack.scrollWidth - menProductsViewport.clientWidth);
      menProductsOffset = Math.max(0, Math.min(menProductsOffset, maxOffset));
      menProductsTrack.style.transform = `translate3d(-${menProductsOffset}px, 0, 0)`;
      if (menProductsPrev) menProductsPrev.disabled = menProductsOffset <= 0;
      if (menProductsNext) menProductsNext.disabled = menProductsOffset >= maxOffset - 1;
    };

    const stepMenProducts = (direction) => {
      if (!menProductsViewport || !menProductsTrack) return;
      const maxOffset = Math.max(0, menProductsTrack.scrollWidth - menProductsViewport.clientWidth);
      menProductsOffset = Math.max(0, Math.min(maxOffset, menProductsOffset + direction * 304));
      syncMenProducts();
    };

    menProductsPrev?.addEventListener('click', () => stepMenProducts(-1));
    menProductsNext?.addEventListener('click', () => stepMenProducts(1));

    const closeLocalizationSheets = () => {
      localizationSheets.forEach((sheet) => {
        sheet.classList.remove('is-open');
        sheet.setAttribute('aria-hidden', 'true');
      });
      localizationTriggers.forEach((trigger) => trigger.setAttribute('aria-expanded', 'false'));
    };

    const openLocalizationSheet = (type) => {
      const target = localizationSheets.find((sheet) => sheet.dataset.helixLocalizationSheet === type);
      if (!target) return;
      closeLocalizationSheets();
      target.classList.add('is-open');
      target.setAttribute('aria-hidden', 'false');
      localizationTriggers.filter((trigger) => trigger.dataset.helixOpenLocalization === type).forEach((trigger) => trigger.setAttribute('aria-expanded', 'true'));
      select('.helix-header__localization-sheet-container', target)?.focus({ preventScroll: true });
      setBodyLock();
    };

    const filterCountryOptions = (input) => {
      const query = input.value.trim().toLowerCase();
      const form = input.closest('form') || root;
      selectAll('[data-helix-country-option]', form).forEach((option) => {
        option.hidden = Boolean(query && !option.textContent.toLowerCase().includes(query));
      });
    };

    countryFilters.forEach((input) => input.addEventListener('input', () => filterCountryOptions(input)));

    const setBodyLock = () => {
      const locked = dropdowns.some((dropdown) => dropdown.open) || localizationSheets.some((sheet) => sheet.classList.contains('is-open')) || root.classList.contains('is-mobile-menu-open') || root.classList.contains('is-search-open') || root.classList.contains('is-cart-open');
      if (locked && !document.documentElement.classList.contains('helix-header-lock')) {
        const scrollbarCompensation = Math.max(0, window.innerWidth - document.documentElement.clientWidth);
        document.documentElement.style.setProperty('--helix-scrollbar-compensation', `${scrollbarCompensation}px`);
      }
      document.documentElement.classList.toggle('helix-header-lock', locked);
      if (!locked) document.documentElement.style.removeProperty('--helix-scrollbar-compensation');
    };

    const closeDropdowns = () => {
      dropdowns.forEach((dropdown) => {
        dropdown.open = false;
      });
      root.classList.remove('is-menu-open');
      root.classList.remove('is-mega-backdrop-open');
      window.setTimeout(syncDropdownState, 0);
    };

    const openLayer = (name) => {
      closeDropdowns();
      closeLayers();
      const target = name === 'mobile' ? mobileMenu : name === 'search' ? searchDrawer : cartDrawer;
      const panel = name === 'mobile' ? mobileMenuPanel : name === 'search' ? searchPanel : cartPanel;
      if (!target) return;
      const stateName = name === 'mobile' ? 'mobile-menu' : name;
      root.classList.add(`is-${stateName}-open`);
      target.setAttribute('aria-hidden', 'false');
      panel?.focus({ preventScroll: true });
      setBodyLock();
      if (name === 'search') select('input[type="search"]', searchDrawer)?.focus({ preventScroll: true });
    };

    const closeLayers = () => {
      root.classList.remove('is-mobile-menu-open', 'is-search-open', 'is-cart-open');
      [mobileMenu, searchDrawer, cartDrawer].forEach((layer) => layer?.setAttribute('aria-hidden', 'true'));
      closeLocalizationSheets();
      mobileDetails.forEach((section) => {
        resetMobileSubmenuTransition(section);
        section.open = false;
      });
      syncMobileSubmenuState();
      select('[data-helix-mobile-open]')?.setAttribute('aria-expanded', 'false');
      setBodyLock();
    };

    const syncMegaPanelHeight = (dropdown) => {
      const panel = select('[data-helix-mega-panel]', dropdown);
      if (!panel) return 0;
      if (!dropdown.open) {
        panel.style.setProperty('--helix-mega-height', '0px');
        return 0;
      }
      const configuredMaxHeight = Number.parseFloat(getComputedStyle(root).getPropertyValue('--helix-mega-max-height')) || 540;
      const maxHeight = Math.min(configuredMaxHeight, Math.max(0, window.innerHeight - 80));
      const panelHeight = Math.min(Math.max(0, panel.scrollHeight - 2), maxHeight);
      panel.style.setProperty('--helix-mega-height', `${panelHeight}px`);
      return panelHeight;
    };

    const syncDropdownState = () => {
      const open = dropdowns.some((dropdown) => dropdown.open);
      const megaBackdropOpen = Boolean(select('.helix-header__dropdown--mega[open]'));
      root.classList.toggle('is-menu-open', open);
      root.classList.toggle('is-mega-backdrop-open', megaBackdropOpen);
      megaScrim?.setAttribute('aria-hidden', String(!megaBackdropOpen));
      let menuHeight = 0;
      dropdowns.forEach((dropdown) => {
        const panelHeight = syncMegaPanelHeight(dropdown);
        if (dropdown.open) menuHeight = panelHeight;
      });
      root.style.setProperty('--helix-menu-height', `${menuHeight}px`);
      setBodyLock();
      syncMenProducts();
    };

    const replayMegaPanelReveal = () => {
      if (!desktopQuery.matches || !root.classList.contains('is-menu-open')) return;
      root.classList.add('is-replaying-menu-reveal');
      void root.offsetWidth;
      root.classList.remove('is-replaying-menu-reveal');
    };

    dropdowns.forEach((dropdown) => {
      const summary = select('summary', dropdown);
      dropdown.addEventListener('toggle', syncDropdownState);

      summary?.addEventListener('click', () => {
        const url = summary.dataset.url;
        if (url && url !== '#') {
          window.location.href = url;
          return;
        }
        window.setTimeout(syncDropdownState, 0);
      });

      dropdown.addEventListener('pointerenter', () => {
        if (!desktopQuery.matches || !dropdown.hasAttribute('data-helix-hover-dropdown')) return;
        window.clearTimeout(closeDropdownTimer);
        const switchingDropdown = dropdowns.some((item) => item !== dropdown && item.open);
        dropdowns.forEach((item) => {
          if (item !== dropdown) item.open = false;
        });
        dropdown.open = true;
        syncDropdownState();
        if (switchingDropdown) replayMegaPanelReveal();
      });

      dropdown.addEventListener('pointerleave', () => {
        if (!desktopQuery.matches || !dropdown.hasAttribute('data-helix-hover-dropdown')) return;
        closeDropdownTimer = window.setTimeout(() => {
          if (!dropdown.matches(':hover')) {
            dropdown.open = false;
            syncDropdownState();
          }
        }, 120);
      });
    });

    mobileSections.forEach((section) => {
      section.addEventListener('toggle', () => {
        if (section.open) {
          mobileSections.forEach((otherSection) => {
            if (otherSection !== section) otherSection.open = false;
          });
        }
        window.setTimeout(syncMobileSubmenuState, 0);
      });
    });

    mobileSubmenuSections.forEach((section) => {
      section.addEventListener('toggle', () => {
        if (section.open) {
          resetMobileSubmenuTransition(section);
          mobileSubmenuSections.forEach((otherSection) => {
            if (otherSection !== section && otherSection.parentElement === section.parentElement) otherSection.open = false;
          });
        }
        window.setTimeout(syncMobileSubmenuState, 0);
      });
    });

    document.addEventListener('click', (event) => {
      if (!root.contains(event.target)) {
        closeDropdowns();
        closeLayers();
      }
    });

    root.addEventListener('click', (event) => {
      const searchTrigger = event.target.closest('[data-helix-open-search]');
      const cartTrigger = event.target.closest('[data-helix-open-cart]');
      const mobileTrigger = event.target.closest('[data-helix-mobile-open]');
      const mobileBack = event.target.closest('[data-helix-mobile-back]');
      const megaCloseTrigger = event.target.closest('[data-helix-close-mega]');
      const closeTrigger = event.target.closest('[data-helix-close-layer], [data-helix-close-mobile]');
      const localizationTrigger = event.target.closest('[data-helix-open-localization]');
      const localizationClose = event.target.closest('[data-helix-close-localization]');
      const countryOption = event.target.closest('[data-helix-country-option]');
      const languageOption = event.target.closest('[data-helix-language-option]');

      if (megaCloseTrigger) {
        event.preventDefault();
        closeDropdowns();
        return;
      }

      if (mobileBack) {
        event.preventDefault();
        closeMobileSubmenu(mobileBack.closest('details'));
        return;
      }

      if (localizationClose) {
        event.preventDefault();
        closeLocalizationSheets();
        setBodyLock();
        return;
      }

      if (localizationTrigger) {
        event.preventDefault();
        openLocalizationSheet(localizationTrigger.dataset.helixOpenLocalization);
        return;
      }

      if (searchTrigger) {
        event.preventDefault();
        openLayer('search');
      }

      if (cartTrigger) {
        event.preventDefault();
        openLayer('cart');
      }

      if (mobileTrigger) {
        event.preventDefault();
        openLayer('mobile');
        mobileTrigger.setAttribute('aria-expanded', 'true');
      }

      if (closeTrigger) {
        event.preventDefault();
        closeLayers();
        select('[data-helix-mobile-open]')?.setAttribute('aria-expanded', 'false');
      }

      if (countryOption) {
        const value = countryOption.value;
        const localizationForm = countryOption.closest('form[data-helix-localization-form]') || select('[data-helix-localization-form]');
        const countryValue = select('[data-helix-country-value]', localizationForm || root);
        if (countryValue && localizationForm && value) {
          countryValue.value = value;
          localizationForm.submit();
        }
      }

      if (languageOption) {
        const value = languageOption.value;
        const localizationForm = languageOption.closest('form[data-helix-localization-form]') || select('[data-helix-localization-form]');
        const languageValue = select('[data-helix-language-value]', localizationForm || root);
        if (languageValue && localizationForm && value) {
          languageValue.value = value;
          localizationForm.submit();
        }
      }
    });

    select('[data-helix-announcement-prev]')?.addEventListener('click', () => showAnnouncement(announcementIndex - 1, -1));
    select('[data-helix-announcement-next]')?.addEventListener('click', () => showAnnouncement(announcementIndex + 1, 1));

    function showAnnouncement(index, direction = 1) {
      const messages = selectAll('[data-helix-announcement-message]');
      if (messages.length < 2) return;
      announcementIndex = (index + messages.length) % messages.length;
      messages.forEach((message, messageIndex) => {
        message.classList.toggle('is-active', messageIndex === announcementIndex);
        message.style.setProperty('--helix-announcement-direction', direction > 0 ? '1' : '-1');
      });
      restartAnnouncementTimer();
    }

    function restartAnnouncementTimer() {
      window.clearInterval(announcementTimer);
      const messages = selectAll('[data-helix-announcement-message]');
      if (messages.length < 2) return;
      announcementTimer = window.setInterval(() => showAnnouncement(announcementIndex + 1, 1), Number(root.dataset.announcementInterval) || 5000);
    }

    function updateScrollState() {
      if (!root.classList.contains('helix-header--sticky')) return;
      const currentScrollY = window.scrollY;
      root.classList.toggle('is-scrolled', currentScrollY > 8);
      if (!desktopQuery.matches) {
        if (currentScrollY > lastScrollY + 3 && currentScrollY > 80) root.classList.add('is-scroll-hidden');
        if (currentScrollY < lastScrollY - 3 || currentScrollY <= 8) root.classList.remove('is-scroll-hidden');
      } else {
        root.classList.remove('is-scroll-hidden');
      }
      lastScrollY = currentScrollY;
    }

    document.addEventListener('keydown', (event) => {
      if (event.key !== 'Escape') return;
      closeDropdowns();
      closeLayers();
      select('[data-helix-mobile-open]')?.setAttribute('aria-expanded', 'false');
    });

    window.addEventListener('scroll', updateScrollState, { passive: true });
    window.addEventListener('resize', () => {
      if (desktopQuery.matches) root.classList.remove('is-scroll-hidden');
      syncDropdownState();
      syncMenProducts();
    });

    restartAnnouncementTimer();
      updateScrollState();
    };

    const bootHelixHeaders = () => {
      document.querySelectorAll('[data-helix-header]').forEach(initHelixHeader);
    };

    if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', bootHelixHeaders);
    else bootHelixHeaders();
    document.addEventListener('shopify:section:load', bootHelixHeaders);
  })();
{% endjavascript %}

{% schema %}
{
  "name": "Helix header",
  "settings": [
    {
      "type": "font_picker",
      "id": "font_family",
      "label": "Typography",
      "default": "figtree_n4"
    },
    {
      "type": "image_picker",
      "id": "logo",
      "label": "Desktop logo"
    },
    {
      "type": "image_picker",
      "id": "transparent_logo",
      "label": "Transparent/white desktop logo"
    },
    {
      "type": "image_picker",
      "id": "mobile_logo",
      "label": "Mobile logo"
    },
    {
      "type": "image_picker",
      "id": "mobile_transparent_logo",
      "label": "Transparent/white mobile logo"
    },
    {
      "type": "range",
      "id": "logo_width",
      "label": "Desktop logo width",
      "min": 90,
      "max": 160,
      "step": 1,
      "unit": "px",
      "default": 110
    },
    {
      "type": "range",
      "id": "mobile_logo_width",
      "label": "Mobile logo width",
      "min": 70,
      "max": 120,
      "step": 1,
      "unit": "px",
      "default": 90
    },
    {
      "type": "text",
      "id": "logo_text",
      "label": "Text logo fallback",
      "default": "HELIX"
    },
    {
      "type": "checkbox",
      "id": "overlay_header",
      "label": "Overlay header on hero",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "sticky_header",
      "label": "Keep header sticky",
      "default": true
    },
    {
      "type": "color",
      "id": "overlay_text_color",
      "label": "Overlay text color",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "solid_text_color",
      "label": "Solid header text color",
      "default": "#323232"
    },
    {
      "type": "color",
      "id": "header_background",
      "label": "Solid header background",
      "default": "#ffffff"
    },
    {
      "type": "color",
      "id": "header_border_color",
      "label": "Header border color",
      "default": "#e5e5e5"
    },
    {
      "type": "checkbox",
      "id": "show_header_border",
      "label": "Show header bottom border",
      "default": false
    },
    {
      "type": "range",
      "id": "desktop_side_padding",
      "label": "Desktop side padding",
      "min": 20,
      "max": 80,
      "step": 1,
      "unit": "px",
      "default": 50
    },
    {
      "type": "checkbox",
      "id": "show_currency",
      "label": "Show country and currency selector",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_search",
      "label": "Show search icon",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_account",
      "label": "Show account icon and login link",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "show_cart",
      "label": "Show cart icon",
      "default": true
    },
    {
      "type": "range",
      "id": "mega_menu_max_height",
      "label": "Mega menu maximum height",
      "min": 360,
      "max": 640,
      "step": 10,
      "unit": "px",
      "default": 540
    },
    {
      "type": "collection",
      "id": "men_featured_collection",
      "label": "Men mega menu product collection"
    },
    {
      "type": "checkbox",
      "id": "show_announcement",
      "label": "Show announcement bar",
      "default": true
    },
    {
      "type": "text",
      "id": "announcement_message_1",
      "label": "Announcement message 1",
      "default": "Free shipping for all orders over 5.000.000₫"
    },
    {
      "type": "text",
      "id": "announcement_emphasis_1",
      "label": "Announcement 1 bold text",
      "default": "5.000.000₫"
    },
    {
      "type": "text",
      "id": "announcement_message_2",
      "label": "Announcement message 2",
      "default": "Receive 20% off your first order. Shop now"
    },
    {
      "type": "text",
      "id": "announcement_emphasis_2",
      "label": "Announcement 2 bold text"
    },
    {
      "type": "range",
      "id": "announcement_interval",
      "label": "Announcement interval",
      "min": 3,
      "max": 15,
      "step": 1,
      "unit": "s",
      "default": 5
    },
    {
      "type": "color",
      "id": "announcement_background",
      "label": "Announcement background",
      "default": "#d3f285"
    },
    {
      "type": "color",
      "id": "announcement_text_color",
      "label": "Announcement text color",
      "default": "#1a1312"
    },
    {
      "type": "text",
      "id": "women_label",
      "label": "Women label",
      "default": "Women"
    },
    {
      "type": "url",
      "id": "women_url",
      "label": "Women link"
    },
    {
      "type": "image_picker",
      "id": "women_banner",
      "label": "Women mega menu image"
    },
    {
      "type": "text",
      "id": "women_banner_title",
      "label": "Women mega menu title",
      "default": "Find your favourites"
    },
    {
      "type": "text",
      "id": "men_label",
      "label": "Men label",
      "default": "Men"
    },
    {
      "type": "url",
      "id": "men_url",
      "label": "Men link"
    },
    {
      "type": "image_picker",
      "id": "men_banner",
      "label": "Men mega menu image"
    },
    {
      "type": "text",
      "id": "men_banner_title",
      "label": "Men mega menu title",
      "default": "The everyday edit"
    },
    {
      "type": "text",
      "id": "pages_label",
      "label": "Pages label",
      "default": "Pages"
    },
    {
      "type": "url",
      "id": "pages_about_url",
      "label": "About us link"
    },
    {
      "type": "url",
      "id": "pages_contact_url",
      "label": "Contact link"
    },
    {
      "type": "url",
      "id": "pages_faq_url",
      "label": "Faqs link"
    },
    {
      "type": "url",
      "id": "pages_store_url",
      "label": "Store locations link"
    },
    {
      "type": "url",
      "id": "pages_recent_url",
      "label": "Recently viewed link"
    },
    {
      "type": "url",
      "id": "pages_journal_url",
      "label": "Our journal link"
    },
    {
      "type": "text",
      "id": "find_shoes_label",
      "label": "Find your shoes label",
      "default": "Find your shoes"
    },
    {
      "type": "url",
      "id": "find_shoes_url",
      "label": "Find your shoes link"
    },
    {
      "type": "url",
      "id": "trending_url",
      "label": "Trending link"
    },
    {
      "type": "url",
      "id": "best_sellers_url",
      "label": "Best sellers link"
    },
    {
      "type": "url",
      "id": "sale_url",
      "label": "On sale link"
    }
  ],
  "presets": [
    {
      "name": "Helix header"
    }
  ]
}
{% endschema %}
